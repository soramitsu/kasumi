# Partition-bound renewable credentials

Source `49c9338448063805989b3fcae8388aa00e8e4388` passed configuration,
authority TLS, ambiguous native mutation TLS, strict workspace Clippy,
fixture-free production check and formatting. Every gate retains the original
finite deadline and one atomic credential snapshot across endpoint retries.
The required installed bearer-file map exactly covers authority partitions.

The manifest records source, lockfile, commands/logs and actual test executable
hashes. The linker diagnostic and remaining remote rotation/drain limitations
are preserved. These focused macOS ARM64 gates are not final release acceptance.
