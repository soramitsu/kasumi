# Indexed historical backup verification

Intermediate macOS ARM64 evidence for `679a8e8`, integrated at `3ee5787`.
All 62 engine library tests and strict engine Clippy passed on this checkpoint.
Historical verification uses bounded encrypted point indexes and canonical typed
records; final restore materialization retains its memory charge through publication.

`evidence.json` records source, executable, and raw log hashes. Every listed log
was hash-checked when copied here. Earlier production/backup/retirement runs have
their actual source qualification; uncommitted fixture diagnostics are explicitly
unbound. The stalled and failed attempts are preserved alongside the final pass.
The finite expiry fixture resolves the original backup session on its publishing
replica before another attempt; it neither increases deadlines nor disables elections.

This is not the 3 GiB capacity/RSS gate or final release acceptance. The fixture's
independent wrapping keys do not establish cross-member backup-key failover.
Aggregate temporary-disk admission and owned blocking cold-history validation
remain separate work at this checkpoint.
