# Independent review of allocator in-place encoding

Reviewed frozen patch a41926e0b6fda54e12eff688da8c88c43d0a0f21c1514395a7f2af79f3fc12f2. Static review supports scoped integration and native qualification; no implementation defect was found in this serializer change. No compilation, tests, formatter, process management or actual source edit was performed by this reviewer.

All three live files exactly match the package's declared base hashes. Proposed files match their declared hashes. git apply --check succeeds on master. Existing decoders, checksums and every checked-length helper are byte-for-byte unchanged. Independently extracted original serializer bodies equal the test reference bodies after replacing only reference method names and removing whitespace; the comparison does not rely on the package's claimed equality.

Production BuddyAllocator and RegionTracker encoders call the complete recursive checked-length pass and exact destination-length check before their first write. All geometry refusals therefore precede output mutation. Bitmap and buddy cumulative offsets, their header arithmetic, child count and each endpoint were checked against usize and u32. Region metadata stores child lengths rather than endpoints: the helper bounds every child to u32 and the total to usize, correctly allowing a representable total larger than u32 on 64-bit targets. The same immutable borrow keeps these checked vectors and scalar lengths stable while nested writers consume successive disjoint tails. Grouped bitmap logical words are bounded by its actual backing. On these supported 32/64-bit pointer-width targets the later casts, multiplication/addition, split_at_mut and slices are consequently within the preflight's proven ranges. No fallible conversion, arbitrary callback, allocation or serialization collector appears after successful preflight.

The canonical writer still emits the same three buddy padding zeros, little-endian scalar fields, absolute bitmap/buddy end offsets, tracker child lengths and exactly the original logical words including last-word unused bits. A child's prefix length is independent of a larger remaining tail; parents subtract precisely the prefix just written. The recursive preflight covers every child before even the parent's header changes. This is an encoding bounds proof, not validation that malformed allocator semantics (for example cross-level logical consistency) represent a valid free-page state; that broader corruption validation is unchanged.

Only one production serializer remains. Old nested-vector algorithms are cfg(test) reference implementations; they are not alternate representations, a decoder or a production fallback. Allocating production to_vec now reserves one complete output, then invokes the same checked encoder. Its second length pass does not construct a child image. Existing infallible panic semantics for invalid internal state remain, now before output allocation. That boundary is explicitly limited and not relabelled as recoverable admission.

The seven proposed tests exercise all supported buddy orders and representative empty/partial/full/allocated/resize states, bitmap word and height boundaries, tracker orders through MAX_REGIONS, unchanged checksums and existing decoder roundtrips. They compare against the original algorithm, guard the destination, test wrong lengths unchanged, observe zero encoder allocations and one output allocation, and reach malformed nested child backing before output writes. These are meaningful source tests but are unrun; static assertions do not establish compiler behavior, observed counts, full admission, total workspace/RSS or maintenance liveness. Original vendor fault and feature suites remain required.
