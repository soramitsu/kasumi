# Combined prerequisite revision4

This71-file cumulative candidate composes two disjoint three-file runtime integration corrections on the preserved revision3. Namespace fixture helpers locate an existing registered physical owner first and use the managed consumer constructor for new directories. Adversarial disappearance/substitution tests retain their later allocation-free failures and additionally test earlier preparation rejection. One new regression creates archive/backup consumers under an installed owner with a live file.

The lifecycle fixture correction admits mandatory fixed bookkeeping before preserving one live denial byte; uses canonical retained opening and verifies original-error address through explicit backend close; and keeps all six independent singleton installation cases with the unchanged256MiB/4096-slot per-installation admission. That singleton case does not prove a shared aggregate budget across six installations. No tests, quotas, ownership fences, or lint requirements are removed. Every original failed run and inventory reconciliation remains preserved.

Actual master is unchanged. Full workspace strict Clippy/check and exact326-case full store runtime qualification remain required for this successor, followed by the existing Python smoke suite. No complete production caller migration, whole memory/RSS bound, or release acceptance is claimed.
