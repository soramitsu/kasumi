# Current-leader observation of the original recovery phase

Target-only proposal for run124 second failure. Its complete14-case target passed12 and failed2. The uncertain-activation case failed inside prepare_completion at the original unresolved phase read, before the activation fault was exercised. After a prior proposal response became Unknown during a term2 change, the original node1 read at line655 reported forwarding to node3; its actual metrics show healthy Follower term3/current_leader3 and applied log47. The failure therefore identifies a stale read route, not a different recovery identity or an unresolved test assertion.

prepare_next already selects the current leader independently of its unused db argument, preserving each original phase/input/context across uncertainty. The fixture then read that phase using a much older db captured near the start. The proposal captures the same read context at its original point, selects the current leader once with the existing helper, and performs the same single recovery_phase read and outcome-is-none assertion. It carries that selected handle into the existing completion continuation. All original databases and physical owners remain strongly held in Fixture.nodes; no storage owner, recovery request, completion command, signed outcome, absolute dispatch deadline or existing assertion changes. No retry loop or timer/stack/quota setting is added. A leader can still change during the read, which must remain a failure. The independent bootstrap-election failure is not addressed.

No actual source edits or runtime execution. The exact original complete14-case receipt and log are124-result.json and124-lifecycle-handoff.log. Full regression and new binary identity must be recorded after integration; prior static-frame evidence only describes its prior binary.
