# Original lifecycle intent deadline: fixture diagnostic

Target-only proposal; not applied or compiled. Terminal129 fails the confirming intent commit at common/recovery_control.rs1776, called from complete_recovery997, with the original Call::wait ten-second deadline. The run supplies no executing-node state or original write-child location at that failure. It does not establish a leadership cause, absence of a commit, or a physical storage failure.

Keep the original request, verified/dispatch-limited context, current-leader selection, single lifecycle_control call, ten-second response deadline and success assertion. Capture only four Copy route fields and a monotonic Instant before that call, after original prepared observation retirement. On failure, print the exact error and elapsed interval, all three actual groups' access/metrics, each local generation's matching intent identity/phase/revision/original lifetime, actual persistent/scratch/memory snapshots and the existing bounded vote history. No new async call, retry, context, request, mutation, drain, or refreshed deadline is added. The same failure then panics. Successful execution takes only the scalar capture/clock overhead; the original post-call intent and phase assertions are unchanged.

Local retained rows and metric snapshots are diagnostic only, not verified authority or atomic causal proof. The failure-only output can distinguish local application and current health, but does not identify a pending proposal's exact await/physical-I/O stage. No definitive cause or fix is claimed. Original failed log/source/binary evidence is retained. A focused successor must preserve all original workload and deadlines; a non-reproduced deadline is not thereby resolved.
