# Root bounded vote-transport diagnostic review

Static review accepts87b828371f16a3d003a63ac8a56b3845f6f906f9c9e909531cf28d8b128d06ea for diagnosis of run124 initial election failure, not as a fix or a timing/performance gate. The two changes are confined to lifecycle integration fixture files. No actual source application or runtime result is implied by this review.

The wrapper retains the existing InProcessRouter and forwards the exact group/source/target/node/request, awaiting and returning its original result. VoteRequest and VoteResponse copies are inline numeric protocol values. Only votes create observations, in32 fixed slots with explicit saturation/eviction/omission counters; pending slots cannot be overwritten. A guard borrows the actual probe through the polled future lifetime and records return versus dropped-future/unwind without claiming remote completion. Candidate metrics and history locks are acquired separately, and no mutex/watch borrow crosses the awaited transport call. Diagnostic text is bounded to128 UTF-8 bytes with explicit truncation, and output occurs only at the original failed leader wait. The fixture still controls the original router for partition/fault behavior. Reopened nodes replace metric receivers only after actual shutdown.

The original100–180ms election,30ms heartbeat,10-second leader wait and router TTLs remain unchanged. The observation is at wrapper return, before OpenRaft's notification handling; published candidate term can lag core state, and a dropped polled future does not identify the specific timeout/cancellation or remote persistence outcome. Source confirms that the recorded metrics.vote comes from io_state.vote (vendor/openraft-0.9.25/openraft/src/core/raft_core.rs619); SaveVote updates that IO state after save_vote().await at1894–1896. This still does not prove receipt of the remote acknowledgement by the candidate.

The extra bounded fixture instrumentation has execution and storage overhead. A success with this probe cannot establish the cause of the prior failure or serve as an uninstrumented performance qualification. Compile and the original targeted/full lifecycle successor remain required.
