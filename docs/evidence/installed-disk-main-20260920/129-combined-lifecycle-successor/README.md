# Root review and composition of lifecycle attempt129 successor

The first failure occurs at prepare_completion's first next_recovery_dispatch read. The original log identifies the executing cached node1 as healthy Follower in term5, with no leader; the same Arc was selected before Start, duplicate Start and alias rejection. Production next_recovery_dispatch uses lifecycle_barrier before observation. Fixture leader() already requires a self-advertising leader and an actual linearizable barrier under its existing ten-second bound.

The six-line first-failure patch is appropriate routing for this observation. It constructs the original finite context before selecting a current leader, so routing consumes authorization lifetime rather than renewing it. The single original read still must succeed. Operation ID, phase ID, request, owner objects and all assertion suffix bytes are preserved. This does not guarantee leadership across the subsequent call or treat any read error as success. There is no retry loop, compatibility behavior or production change.

The second independent hunk adds failure-only diagnostics to the original single CommitIntent call. It records four scalar route fields before the call, then exact original error, original identity/phase and elapsed time if it fails. It records each actual group's access/metrics, locally retained matching intent and persistent/scratch/memory snapshots, then the bounded vote history, and still panics. Local rows are diagnostic, never a verified authority result. This does not identify the exact pending child stage or prove that the original intent was committed or absent.

Snapshot API limitations: these are synchronous observations that take existing locks and query filesystem space. Existing poisoned-lock recovery may mark an owner failed or uncertain. Therefore the diagnostic is not entirely side-effect-free and its reporting duration is not bounded by the original request timeout. It does not call observed child census, pause, reconcile, close, drain or any release of credit. No original request deadline, context, phase, command, workload or success assertion is changed. The original frozen diagnostic README's blanket claim of no mutation is qualified by this receipt; its historical bytes remain unchanged.

Composition verifies all files in both input manifests, verifies the exact common df389a baseline in actual source, extracts both edits against that common base, rejects overlapping ranges and applies both distinct edit sets once. Root source stays unchanged. The new diff is the union of the original two patches; neither source hash is incorrectly treated as the other's base. Native tests and compiler checks are still pending after authority134 drains. Independent diagnostic review will be linked separately before actual source application.
