# Root review of the generic directory correction

The specific new-acquisition finding in the preceding root review is resolved in this four-file successor. Nonroot open_directory dispatches to the managed retained path before any newly introduced native descriptor acquisition. PendingDirectory holds preallocated path/ancestry/owner backing and the counted slot; exact ledger-bound descent fills that ancestry and retains both possible temporary descriptors. The final parent descriptor moves into retained storage before registration; every fallible subsequent path leaves the operation installed. Healthy final absence closes/retire resources before credit. Ordinary verification checks retained parent metadata without introducing the previously added generic parent traversal.

Reviewed the full successor and both added tests. The new fault test closes the actual descriptor then injects EIO, asserts the exact retained operation and proves reconcile cannot erase the close uncertainty. Native01 passes the unchanged previous47 cases plus both new cases (49 pass; five inherited device cases filtered), with unchanged source/binary inventories and all groups reported drained. Exact51-file cumulative bases still match the actual checkout and git apply --check passes. Original failed/reviewed packages remain preserved.

Scoped primitive approval; remain unapplied pending coherent caller adoption. This resolves the newly introduced generic nonroot acquisitions, not the remaining ordinary root acquisition/verification/last-owner Drop contract. Whole session and permanent terminal capacity admission, all44 caller sites, physical pre-effect directory bounds, all memory/native/RSS accounting and integrated release qualification remain open. No compiler/native process or actual-source edit was dispatched by this review.
