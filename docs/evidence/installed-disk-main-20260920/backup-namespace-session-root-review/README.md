# Backup namespace and terminal admission: root integration review

Status: APPROVED_PREREQUISITE_PENDING_COMPOSED_WORKSPACE. This review authorizes composing the concrete namespace/session prerequisite with the census/opening prerequisite. It is not full production adoption or release acceptance.

Root read the finite six-part admission, file/directory reservation transfers, physical session claim, terminal codec and streaming read/write paths, metadata formula, backup/audit constructor changes, restart and exact classification-race regressions. Earlier directory/generic prerequisite reviews remain their own evidence; this review does not relabel all 57 files as newly reviewed.

The session reserves both fixed terminal inodes before first publication, transfers actual preallocated owner backing without a second extent charge, and publishes each terminal by moving its original reserve inode. Fixed canonical framing rejects old/unframed input, nonzero reserved/header/tail bytes, wrong session/slot, incorrect length and digest. Classification uses one 4 KiB buffer. Reads enforce the caller payload limit before allocating exact ciphertext, without fencing a valid record merely for a small caller limit. The metadata formula charges the declared terminal buffers/header/hash state per admitted file owner; it does not prove total RSS or all caller allocations.

Root identified the pre-claim owned path and UUID String allocations in candidate03. Revision4 fixes this with a retained DirectoryOwner prefix and stack UUID components under the same State guard. Its claim is declared before owned put paths, so those paths retire before lane reuse. The shared physical lane covers the absence/reserve classification through immutable publication, excluding the demonstrated two-wrapper reserve-recreation race. Accepted and contended claim regressions measure no path allocation. Returned diagnostic allocations and earlier async worker/ciphertext ownership remain separate open requirements.

Root verified all 57 cumulative source bases against master and all proposed hashes, all 455 frozen package artifacts, five successful native phase receipts and their exact logs, unchanged before/after input inventories, and the 67 distinct selected test names with recorded drained groups. This rechecks author runtime evidence; root did not rerun Rust. Full store/server compilation, backup/audit constructors and authenticated GC integration are deliberately not inferred from the scoped harness, which excludes three authenticated GC methods. The combined workspace qualification is the next gate.

Remaining release work includes complete async-worker/ciphertext/error/stack admission, inherited file/directory descriptor close-outcome custody, all remaining directory consumers, filesystem-specific pre-effect bounds, full NodeDatabase caller adoption and complete transaction/cache/proxy workspace. Existing caps, workload sizes and native deadlines are unchanged. No source change or compatibility path is introduced by this review.
