# Aggregate filesystem session admission — target-only successor

This package is not installed in actual source and is not first-release acceptance. Work is restricted to `/Users/mtakemiya/dev/kasumi` on `master`; all edits here are under `target/installed-disk-validation/`. The base is frozen `directory-managed-namespace-generic`, manifest `db42b5c363ec219f60bf4d67af7b599adec3da532231777a18ec1191fd1d1f82`. Its cumulative directory/census/fixed-map prerequisites remain required.

Current frozen candidate: `candidate-03.patch` (`d7392ce6c5e0cd411af096ba65351c0ba16f2aaafc1d0f09db034dcad2f941f1`) and `candidate-03.json` (`16b112ad641fd9dadaeac0274418d5d319400a972fda8123548ba331ed39de66`). The thirteen-file successor applies to the copied base with exact post-apply readback. This README is progress documentation; the final package has not been frozen.

## Complete admission and resource transfer

One State-owned record holds at most six typed parts: three missing session directories, two permanent terminal files, and the current object temporary. Counts are operation shape bounds, not reduced installation F/D/H/depth/name/byte policies. Owner/cardinality shortages reject before allocating future owner Arcs. Exact names and actual uninitialized FileOwner/DirectoryOwner Arc backing are allocated under the already-held installed metadata reservation, with fixed bank and parent-child capacity checked before aggregate byte admission and before the first planned descriptor or namespace effect.

The complete byte demand enters the existing NodeDisk/device promise ledger once. Future file/directory/owner/map/parent-entry slots remain unavailable to ordinary creators. A typed single-use permit transfers its actual owner Arc plus promise into the existing file Budget/inode record or retained PendingDirectory. File creation starts with actual length zero and its complete admitted reserved length; later reserve_growth within that allowance has zero additional byte charge. File growth and synchronization must materialize the exact intended length before finish. Directory creation skips the ordinary second reserve because the batch has already funded its full directory-policy allowance.

The batch witness is not the resource owner. State keeps all unconsumed paths, actual Arc backing and promises through abandonment or a failed operation. Dropping an active witness fences the owner. A pristine explicit cancel destroys its actual backing before returning shared/local credit. A partial operation cannot cancel. Census requires actual witness drain, keeps the old batch across failed/canceled candidate scans, and retires it only after complete candidate acceptance. The existing independently retained PendingDirectory close protocol remains intact.

## Physical session claim

A nonblocking State-owned namespace claim begins before the first terminal classification and spans directory/file preparation, full aggregate settlement, terminal/object write and immutable publication. The accepted session resources created after the claim retire before successful claim credit. Root review identified pre-claim slot.relative/claim_path allocations in candidate03; see the explicit limitation below. There is one mutation lane per physical NodeDisk, matching the existing single retained batch model. A second wrapper receives WouldBlock before classification, with no new waiting worker or extended deadline. Failed claim metadata remains until witness drain and census.

Root review caught a TOCTOU in the initial late-file-claim design: one wrapper could observe published absence, another rename the reserve to published, and the first then mistake reserve absence for an unprepared session. The shared earlier claim closes that gap. The regression pauses exactly after published-name classification, before reserve-name observation, and challenges it from a second wrapper.

## Permanent terminal capacity and canonical representation

Before any new session intent or object publication, both `intent.reserve` and `outcome.reserve` exist as durable fixed-length regular inodes outside `objects/`. Each terminal has 2,162,772 bytes of maximum ciphertext allowance (unchanged 64KiB record bound + 2MiB encryption header + 84 bytes), plus a 72-byte canonical container header: **2,162,844 logical bytes per inode**, rounded by the installed filesystem unit. These permanent inodes consume F and real logical extent/pending promises, so a fresh process census reconstructs future terminal capacity without a RAM ticket or a logical metadata reservation.

Publication writes the matching sole reserve owner and renames that exact inode to `intent.kasumi` or `outcome.kasumi`. It does not allocate a later inode or grow beyond the admitted length. Fixed extent remains after publication. Reserve files are not cleanup objects or serving authority. Existing intent/outcome/session identities remain permanent; authenticated deletion still addresses only UUID objects under `objects/`.

The one first-release representation uses `KSMSES01`, exact expected session UUID/terminal tag, checked used length, ciphertext SHA256, exact fixed total length and canonical zero padding. Unknown/unframed representations reject; no legacy decoder was added. The public/S3 ciphertext representation is unchanged. Filesystem get returns the exact original encrypted bytes for existing cryptographic and abort-proof checks.

Candidate03 streams classification through one 4KiB buffer instead of allocating a complete fixed container. A terminal read checks its actual payload length against the caller limit before allocating exactly that ciphertext length. The per-file-owner metadata envelope includes buffer/header copies and SHA state, including a prepared owner slot; the payload uses the existing caller byte bound. A small caller limit rejects without marking valid disk content corrupt. Actual allocator/native/call-stack/RSS qualification remains separate.

## Caller migration and test scope

`backup_sessions_fs` uses NodeDiskDirectory for open/sync/child access, and session put uses the complete admitted plan. FilesystemBackupDestination and FilesystemAuditArchive initialization directly use retained, admitted directory creation. The archive retains its directory owner. No raw mkdir/openat/canonicalize fallback remains in these migrated initialization/session paths. The broader frozen 44-site directory caller inventory is not closed by these three callers.

The proposed module cohort selects the same original 49 directory/census/fixed-map/cursor/ledger tests, seven batch tests, two existing filesystem leaf-ownership tests, five session tests, and two codec tests: **65 selected tests**, five inherited device tests compiled but filtered. The session tests include a real two-process restart at full file and byte limits, exact original-inode terminal publication, all-terminal shortage before mkdir, and the paused two-wrapper classification race. Existing tests now drain the newly real directory owner before census and retain the configured root's directory allowance afterward. Within the terminal regression, classification and small-limit rejection are checked for zero heap allocation.

This standalone harness compiles actual proposed NodeDisk and filesystem open/put/get/terminal code. It extracts the exact unchanged BackupSessionSlot definition. Three authenticated GC methods (`aborted_objects`, `list`, `delete`) are omitted only in the harness and their exact excluded source is hashed. No fake proof or permissive StorageAccess implementation is supplied. The real candidate retains all three methods, including canonical outcome unwrapping before proof verification. Full store compilation, backup/audit constructor integration and authenticated GC tests remain required.

Every native command keeps the original 120-second deadline. Every compiler/test process group, executable and source/dependency inventory is recorded, and no retry changes the test/filter/resource scope. The native window is coordinated with root/opening; no actual source is changed.

## Preserved trials

- Native01 dependency preparation failed before any compiler/test dispatch: an available prebuilt store rlib referenced an unavailable old redb fingerprint, and aliased dependencies also lacked direct name matches. No stale dependency or replacement authority was accepted. Partial copied artifacts and an exact inventory remain under `native-01`.
- An outer `os.setsid` attempt was rejected with EPERM before run.py executed; the tool process already owned a session. The plain runner and each independently monitored child group then ran as designed. `native-02/launcher-failure.json` preserves the event.
- Native02 production compilation failed with seven E0277 errors in unused extracted BackupSessionObject/Page serde definitions; the frozen UUID dependency lacks serde. No tests ran. Runner PG39140 and child PG39141/39142 drained; all 1,817 inputs were unchanged. Result SHA `cd72a604a48c237ffa5973c659ba6688bc683168c167c50b97cb650180519cfd`.
- Candidate03 extracts only the actually used exact Slot definition and adds the streamed/charged terminal workspace. No test, payload/file bound, quota or deadline was reduced. Native03 passed all 65 selected tests (5 inherited device tests filtered) in 4.58 seconds. Both compilation phases were clean. Runner PG52452 and child PG52464/52465/52477/52496/52502 all drained; all 1,817 inputs and executed binary hashes were unchanged. Results SHA `5aa980589cd17941c66a19089046e296b87d465eb3d83f35fd88340b41666523`. Exact command, inventory, executable and terminal-drain receipts remain under native-03.

## Still required

Root's candidate03 review found that slot.relative() and claim_path allocate before the claim, and claim_path retires after the claim. A single accepted lane does not bound those paths across callers waiting for State. No preexisting admitted caller bound was established: the current async session_put checks ciphertext length and then spawn_blocking without such a bound. The candidate is therefore not proof of the complete per-call path/worker envelope. A distinct correction can derive the claim from the retained DirectoryOwner binding and a stack UUID encoding before allocating paths; current candidate03 remains frozen. Exact review context is retained in candidate-03-call-envelope-review.json.

This work does not close inherited generic file preparation and FileOwner/RetainedParent drop paths that do not retain every native close result. The inherited ordinary directory Drop/configured-root acquisition close qualification also remains open. Whole MemoryCore/census installation/adoption, all remaining directory callers, original-deadline worker retention across every service boundary, supported-filesystem pre-effect physical directory bounds and complete memory/RSS/allocator qualification remain first-release prerequisites. No release-complete or complete drainage claim is made.
