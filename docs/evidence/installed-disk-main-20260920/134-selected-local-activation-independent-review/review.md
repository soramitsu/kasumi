# Independent selected-voter activation confirmation review

Verdict: approve the bounded fixture handoff for coordinated compile/format and original-case verification. No source-review blocker found. Attempt134 remains a failed full authority run (62 passes, one failure); this proposal is not a passing result or an explanation of the underlying election movement.

The failure is specifically TargetJournal::record_activation's sign_activated call, after activate_target and original sign_activated had succeeded and after every other voter was confirmed. The logged linearizable-barrier forwarding failure matches VerifiedTargetActivation::release's original leader-observation route. A permanent selected-voter journal projection should use that voter's exact persisted application confirmation after the earlier distributed work. This is an additional observation with the existing API, not an instruction to accept UnknownOutcome.

The original leader-bound completed proof, accepted issuer receipt, successful signed activation, forged-signature checks and every follower confirmation are unchanged. The same actual_signed_activation and selected.operation are passed once to confirm_target_activation; the added call must succeed. The selected local fact equals completed.fact, and the observer ID must equal selected.id. Both initial journal publication and exact replay use this one local proof and the same selected signer. Original projection execution, journal reopen/substitution, lifecycle-serving denial, target reopen and operational membership/suspension assertions are byte-preserved. Mechanical removal of exactly the inserted block/drop and reversal of the two argument substitutions reproduces the original file byte-for-byte.

Production checks remain substantial. local_target_activation_observation checks the original operation and target Activate phase, verifies the existing signed activation, requires the actual local completion and activation to equal that signed history, checks exact phase input, and confirms real committed/applied custody coverage. It checks the persisted membership's voter IDs, addresses and count plus revision coverage. confirm_target_activation stores the original signed fact and the operation's release fence. TargetSigner::sign_activated checks signer/observer identity and releases before and after signing; TargetJournal::record_activation releases again after publication. No activation proposal, new issuer selection, fresh leadership requirement, retry loop, credential, phase identity or deadline is introduced by this handoff.

TargetReleaseFence retains the same Arc operation identity, deadline, cancellation token and invocation; it grants no renewed lifetime. Both completed and local_completed remain owned until the explicit boundary, where local_completed is dropped before completed and before close_targets. The selected DB remains owned by the fixture/proofs, and this proposal adds no detached task or altered shutdown path.

Limits: local confirmation is not fresh quorum authority, and the API deliberately checks original phase/access fences and actual persisted state on every release. A newer persisted application term can change observed_term and make release fail; the proposal does not promise indefinite or unconditional local-proof reuse. A failed storage/core, invalid signature, missing or different local fact, changed membership or expired original operation still fails. The initial signed activation still must originate from the original successful leader observation. Broader election/latency causes and full release qualification remain open.

Reviewed paths: target_activation_service.rs19-40 proof release,43-60 access,62-109 leader observation,277-365 local confirmation; target_projection.rs290-300 publication/signing/release; target_signer.rs370-403 exact signer release; target_invocation.rs458-485 immutable release fence; local_applied.rs29-78 actual committed/applied membership coverage; service.rs899 materialization access. Exact pinned proposal and terminal evidence hashes matched before/after. Target-only apply-check and exact application reproduced the proposed bytes. Nine current source hashes stayed unchanged. No compiler, Cargo, native program, debugger or test ran during this review.
