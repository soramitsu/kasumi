# Bounded allocation purge independent review

No source-review blocker identified in the exact three-file 78a20485 candidate. Approved for scoped integration and original vendor gates, not as complete physical-maintenance admission or finite-workspace qualification. This reviewer made no actual Rust edits and ran no compiler, test or native program.

The baseline chain is exact: canonical f2103f19, then compile correction 6eb30fe4, then this candidate. The correction's two bases equal canonical proposed outputs; all three purge bases equal their final dependency outputs. A target-only apply/check reproduces every proposed hash. The restored complete TableNamespace block and the stats/print_debug/transaction tail are byte-identical. The sole public-or-crate-public method-name change is the direct private preview rename; an overlay scan found no old call or alias. Public Database::compact retains its signature and both fixed two-commit helpers.

Historical DATA pages remain physically allocated through preparation and winning header publication. Purging allocation metadata removes references, not pages. A commit selects at most400 allocation IDs in complete checked canonical records. Any additional eligible allocation record blocks all historical DATA selection/freeing. If allocation eligibility is exhausted, it selects and validates DATA before extracting either prefix. Both plans bind role, exact root, horizon, last key and counts; extraction rechecks them under the same system namespace lock, with explicit close. The DATA collection remains the original inline400 array; no additional page-ID Vec or simultaneous second extraction owner is introduced.

The surviving-savepoint case is safe under the existing canonical writer invariant. A surviving savepoint at S pins a real read reference at S, so selected DATA free generation F is at most S. A committed historical allocation A naming that freed page precedes F: allocate-and-free within one transaction is handled immediately by PageAllocator::free_if_uncommitted/conditional_free, rather than the historical DATA queue. Thus A<F<=S, making its allocation record eligible below S. Records retained at A>=S cannot name the selected DATA prefix. DATA's horizon is frozen first; a concurrent savepoint/reader drop may advance eligibility but cannot widen that selected prefix. Persistent deletions are accounted for at the post-commit savepoint horizon while their read reference conservatively pins the earlier DATA horizon. The exclusive committing writer prevents a new earlier savepoint from appearing between these steps.

Preview is conservative for both queues: every unselected row, including an ineligible first row, reports debt. If allocation debt blocks DATA, a selected small DATA prefix is also unfinished. An empty maintenance commit adds no user allocation/free rows, and the writer excludes other mutations of these roots. Reader/savepoint release can produce an extra conservative observation but cannot make false mean that unselected reclaim debt was hidden. Finite pre-existing eligible allocation history makes bounded prefix progress before DATA progress; concurrent production may outpace it, so eventual completion is conditional.

Malformed selected allocation or DATA is rejected before either extraction and poisons publication. When allocation debt blocks DATA, DATA is deliberately not selected or mutated in that commit. Capacity preparation failure retains the existing whole-transaction rollback/latch path. Positive abort clears the deferred array only after allocator rollback and restores the original roots through transaction discard. Physical failures retain the existing owner-failure and retained-transaction semantics. The new allocation-close fixture forces actual growth/sync failure and preserves the original I/O marker, actual transaction/database/file, Retained abort report and failed owner without replay. The unchanged generic extraction helper preserves the first returned cause; its pre-existing lack of a full independent multi-cause close/panic outcome aggregate is not solved by this prerequisite.

All ten original tests are byte-preserved except direct private method migration. Six new tests cover817 real allocation/free IDs with cold reopen after every400/400/17 transition, both malformed selected namespace orders, savepoint release after preview, a held user reader, persistent savepoint payload restore/deletion, and the actual allocation-prefix close fault. Original DATA close fault, exact physical allocation-until-commit, abort restoration, retained custody and obsolete-format rejection remain intact. These tests were inspected only; this receipt does not claim they compile or pass.

Open release work remains explicit: a typed restricted maintenance permit, protected allocator free-page and file-extension reserve, exact COW and allocator-rebuild workspace accounting, general NodeDatabase caller custody/adoption, and integrated execution. Existing all-history savepoint restore and allocator reconstruction are not bounded by this400-ID phase. Original RAM/disk policies, deadlines and first-release format rejection remain unchanged.
