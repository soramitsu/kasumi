# Root review of canonical allocator key rejection

Scoped approval for integration and runtime qualification after composing with the retained-opening changes. This review does not qualify the full allocator decoder or workspace bound.

The private Deprecated variant and tag0 writer disappear. The checked parser admits only fixed five-byte region keys and exact zero-padded tracker/transaction keys; Value's infallible adapter has no alternate decoder. The raw fixed-width tree traversal checks leaf key bytes, branch separator bytes, page bounds, monotone value ends, strict leaf ordering and eight-byte transaction stamps before a typed allocator comparator or stale-stamp decision. The inline path rejects ancestor cycles and caps depth at the existing tree limit. Validation is wired into allocator opening, unclean winning-root checksum verification, repair before allocator reset, and read-only unclean refusal. Existing supported stale snapshots remain eligible for verified repair; malformed/unsupported key encodings do not become that path.

Reviewed the tests' deliberate below-writer publication, real clean/unclean files, zero callback and unchanged-byte assertions, malformed stamp cases, successful stale repair control, raw page geometry and real in-memory branch/cycle cases. Their execution remains pending. The raw helper's offset arithmetic is bounded by checked metadata geometry before indexing. Region index versus actual layout, branch separator versus child range, complete buddy/tracker payload validation, page-cache/workspace and native stack bounds remain open. New error strings are allocated and require the complete caller admission contract; zero-allocation claims apply only to successful scalar key parsing.

Retained opening's constructor remains a separate prerequisite; its canonical path calls the same primary/get-allocator boundaries updated here. Composition must preserve both implementations and the latest test-lint fixes. No actual source, compiler or native process is changed by this review.
