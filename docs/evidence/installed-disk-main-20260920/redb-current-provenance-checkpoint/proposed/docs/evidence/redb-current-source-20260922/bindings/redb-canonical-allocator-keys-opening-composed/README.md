# Canonical allocator keys composed with retained opening

Target-only preparation on /Users/mtakemiya/dev/kasumi master. No actual source edits, Cargo invocation, compilation, or native tests by this agent. This is the six-file canonical-key rejection change composed against the exact ten applied retained-opening revision3 files.

The original canonical patch a61e1c3d9ac6bfef19a4f33b669d9355c59fca38af02234817244a8ad02551d4 and original opening patch a28a3b438ed3138afb1c0faec7daab045f0812800ebf696b8ce8a93d86fd3cef remain frozen and unchanged. The original canonical patch applies without conflict to a target copy of current opening source. Reversing that same patch from a second composed target copy produces exact current source bytes for every old file and removes only the two added validator/test files. No hand-written implementation change was introduced during composition.

Opening's retained cache and memory constructors, admission proxy, retained bootstrap and close state machine remain exact. All four overlapping opening files (db.rs, transactions.rs, tree_store/mod.rs, page_manager.rs) retain opening's additions. The other six opening files are pinned unchanged. In db.rs the canonical raw validation is in get_allocator_state_table before typed traversal, verify_primary_checksums before verification, and rebuild_allocator_state before reset. RetainedDatabaseOpening::open_body already calls primary_verifies and get_allocator_state_table before begin_writable; its repair route reaches do_repair/rebuild_allocator_state. Existing consuming and read-only entry hooks remain covered. The original canonical parser, raw walker, defensive exact-eight-byte transaction stamp conversion, and all seven test bodies remain the identical source changes.

Validation performed here: exact opening base hashes, exact original key patch hash, forward/reverse target composition, target-only rustfmt check, repository git apply --check, and actual-source unchanged checks. Parent owns native integration and test results. The original canonical package README describes prepared tests and deliberately remaining limits; it is copied as canonical-key-scope.md.

This validates canonical key representation and bounded raw page slices only. Branch separator correspondence to child ranges, region-index correspondence to current layout, and buddy/tracker payload validation remain open. Raw traversal retains ancestor page guards/cache/stack and does not prove the total memory bound. The infallible internal Value decoder is protected by mandatory prevalidation at opening/integrity boundaries. No obsolete keys, aliases, variant, or writer remains; no compatibility path is added.
