# Retained file custody handoff

Candidate 04 is ready for composition and broader qualification. It is unapplied and lives entirely under the requested Kasumi master checkout. Actual `crates/`, `vendor/`, Cargo inputs, and HEAD were unchanged.

- Final nine-file patch: `custody.patch`, SHA-256 `48756df32f870731310a1cec7fc665c1311ea6b7aec4ef05a37f5bc42fb516ca`.
- Manifest: `manifest.json`, SHA-256 `89bf760a949096fe2c9c21a1907342bf21ecdfcdc5196f5e68785ba40800708d`.
- Qualification: `qualification-receipt.json`, SHA-256 `ed394cea3d56f1b1e8386936d20d22870da9765b13417f6f4842975f8bf8d895`.
- Final native terminal receipt: `native-01/terminal-drain.json`, SHA-256 `7d479a7618aaa1d395d38cc00b79242c9849065da9ab0690c8074db23c2e70b9`.
- Frozen raw evidence: `raw-manifest.json`, 1,384 files, SHA-256 `4c6021f6fda7add99b849b8d08a03e9a6cef72d3450ceea7c09d072aa3021942`.

Production and test compilation, exact binary inventory, production metadata geometry, and all 132 selected tests passed. Runtime was 8.27 seconds; the six phases totaled 11.298 seconds and retained their original 120-second individual caps. All 1,825 source/compiler/dependency inputs matched before and after. Runner and process groups 31259, 31260, 31262, 31266, 31270, 31271 and 31272 drained without signals or timeouts. The serial native window was explicitly returned.

The fixed H+1 bank retains failed preparation/live-owner payloads, original operation/native close outcomes, and exact registered Weak backing. Every acquired file/walk descriptor enters admitted custody before inspection. Explicit one-shot close precedes credit; uncertain descriptor integers are diagnostic-only and never retried. Complete accepted census after external drain is the explicit terminal-attempt relinquishment boundary. A Failed owner cannot start another file-verification descriptor walk. The five new regressions cover unknown close, retained diagnostic identity across canceled census, verification-walk close failure, retirement unwind, and the Failed acquisition fence.

The production change has scoped independent review in `../../file-owner-custody-independent-review/receipt.json` (SHA-256 `0c2fab00c7132f60aa950a15d42bfa5af5b1976ba0ab3b32a84d2dcaf4b3eb64`). Candidate 03/04 change only fixtures after that review. Their final scoped review, including all native receipts and exact 132 selected names, is `../../file-owner-custody-independent-review/candidate04-fixture-native-review.json` (SHA-256 `690ea6a5263c97b81cef25bf3ab4bc2e1bad449f2eb9c2fc99c1069e52342ba8`). The reviewer rehashed all 1,637 in-root input files and compared the 188 external toolchain records without reopening those external paths. The first test compile failure and the later 131-pass/one-failure run are preserved with unchanged inventories and actual process drains; no trial or assertion branch was removed. Candidate 04 keeps the failed-owner fixture's original inode/EOF proof and adds complete content verification through an independent observer acquired before the injected fault.

The patch is relative to frozen `backup-namespace-admission-revision4/proposed`. Compose it with the now-qualified `storage-and-namespace-integration-revision6` rather than overwriting those newer files. Overlaps are NodeDisk/file/parent/memory/test modules; retain the integration package's lint corrections and fixture-owner lookup. The custody patch also includes the four earlier adversarial fixture-order corrections, whose provenance is in `fixture-composition.json`. The two new modules are `node_disk/native_file.rs` and `node_disk/file/custody.rs`. A read-only three-way preview found five clean merges, the two new paths, two conflict blocks in `node_disk/file.rs`, and one in `node_disk/tests.rs`. `composition-preview.json` (SHA-256 `891d1ebb1a53a26431d4d80ce3265b5d978abe45c4b9f7b061ef329b4f202633`) binds those inputs and preview outputs; it does not qualify a composed source tree.

Full composed workspace check, strict Clippy and store/engine/server tests remain necessary. The standalone linker explicitly omits five existing ScratchDisk/NodeStore integration cases, retains the earlier directory/namespace integration exclusions, and excludes the same three authenticated GC methods without substituting authority or StorageAccess stubs. Its five inherited device cases compile but remain filtered. The metadata result leaves 704,750,622 bytes below the unchanged 2 GiB policy after its stated reservations; this is not a whole-process memory/RSS proof.

Configured-root acquisition, inherited DirectoryOwner Drop and directory verification closure remain separate open custody work. File callers exclusively use the retained `verify_file`/`retire_drained` protocol. Opaque returned-report/panic/caller envelopes, actual allocator/native-mutex RSS, and supported-filesystem pre-effect physical bounds remain unqualified. No first-release completion or full drain/RSS claim is made.

The independently reviewed native trial had no timeout. The frozen module runner does omit `timed_out` from its continuation/final acceptance predicates, so it must not be reused unchanged: a separately frozen successor must require `not timed_out`. This does not affect the completed trial, whose six flags were all false. The raw manifest covers the frozen source/harness/trial evidence; this final handoff, qualification, composition preview and external independent-review receipts are separately bound.
