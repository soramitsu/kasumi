Scoped approval: no blocking finding in the frozen revision 4 six-file claim-path delta. This addresses the previously identified owned String/PathBuf allocations before namespace claim admission. It is not a release acceptance or a complete review of the inherited namespace-session candidate.

The production helper validates nil session/object UUIDs, formats the non-nil session into a 37-byte stack buffer and passes borrowed CStr components. There is no slot.relative String or claim_path PathBuf before the claim. Non-v4 non-nil UUIDs remain accepted. The helper cannot be paired with another NodeDisk: it derives the disk, root, retained path prefix and inode identity from the private retained DirectoryOwner.

claim_descendants holds that disk's State while it verifies the settled directory ledger binding, exact component/name/depth constraints and folds the proposed namespace binding. claim_binding_namespace uses the same State guard for phase/device eligibility, contention, checked generation and inline claim publication; it neither re-locks State nor allocates names. The inherited device lock may block, as may acquisition of State. The claim contention result is WouldBlock, not a claim of globally nonblocking operation. Existing component-count and name limits remain unchanged.

put declares the accepted claim before every newly owned operation path, directory/file owner and admission value. Reverse lexical destruction and inner temporary scope retirement therefore precede destruction of the claim on normal return and early error. The inherited claim destructor credits the lane only with Open state, no pending batch and no pending directory; otherwise it retains an unwitnessed claim record and fails the owner. This review does not upgrade inherited native-close custody to proven physical retirement. Returned anyhow diagnostics and caller-owned ciphertext/resources have separate lifetimes.

The two added regressions meaningfully cover allocation-free private-helper nil rejection, accepted acquisition, busy rejection, the exact returned-anyhow allocation baseline for public contention, healthy lane reuse after a real terminal-header error, root/nested retained prefix and Arc identity, invalid component encodings, inclusive name length and strict depth boundaries. There is no new dynamic test pausing a caller while another holder owns State, nor an allocator deallocation observer for all put locals. The static path and declaration-order review support the bounded owned-path conclusion.

The author's native-01 evidence was read and hash-checked after completion: five phases exit 0 without timeout and report drained groups; all 67 selected tests pass, five inherited device tests filtered; 1,817 before/after input entries compare equal. The frozen 93 native input bindings, run logs and executable-before/after identities are consistent. This reviewer did not dispatch native/Cargo, inspect binaries or independently repeat those tests. The author retained a corrected parser receipt because one test emits an extra line before its success token; the final exact-name inventory contains 67 unique tests and the aggregate reports zero failure.

Scope remains open for whole-call worker/stack/ciphertext/error admission, total resident memory, complete store/server compilation and strict lint, authenticated GC integration, full constructor/adoption wiring, inherited file-helper native-close gaps, and supported-filesystem pre-effect directory bounds. No source, manifest, branch, worktree or native execution was changed by this review; only this target review package was written.
