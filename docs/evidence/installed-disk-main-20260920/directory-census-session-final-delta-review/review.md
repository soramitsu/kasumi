# Census final delta review

Approved for the bounded prerequisite integration, with integrated gates still required. This receipt binds review-snapshot-04, preserves the earlier snapshot02 review unchanged, and includes the private publication-tail correction identified by the redb reviewer. No actual source edits or Rust/native execution were performed by this reviewer.

The snapshot02 Preparing-to-Installed tail could release the resident charge during Arc payload destruction before its backing allocation retired. Snapshot03 adds private ProvisionalOwner custody. Only Deref<T> is exposed; no Arc/Weak leaves the provisional phase. Error or unwind takes the unique Arc through into_inner, retiring its allocation before dropping the returned NodeDisk fields and final lease. The guard remains armed while the existing registry entry receives its clone. Installation replaces only the inline Transition state, followed by a nonfallible take; no callbacks or fallible work intervene after installation. Promise and state guards leave scope before provisional destruction. This proof depends on the private no-Weak/no-Arc-escape invariant, not a count heuristic.

The real registry test observes the actual Box<Entry<RegisteredDisk>> allocation through the existing thread-local System allocator observer. Its lease callback requires completed deallocation and closed original root/ancestor fds before returning original credit. The separate provisional test observes the exact pinned-std Arc allocation on returned EIO and original panic, checks requested layout/count and original outcome, and verifies lease retirement afterward. The observer does not dereference the retired allocation or call application code from the allocator; the borrowed observation stays alive for the entire synchronous invocation and unwind.

The actual constructor overflow test reaches the post-census checked promise publication failure, verifies that its unpublished registry entry is removed, and proves no additional admission remains before reopening normally. Snapshot04 addresses the shared-registry contention caveat: existing retry_disk_registry handles only RegistryBusy, while a retained DeviceDisk anchor preserves the same u64::MAX promise across attempts; each attempt owns its zero-pending registration. The original overflow remains required. The anchor is explicitly reset and dropped only after failure accounting assertions. This does not retry an accepted census/storage failure or change the helper's original five-second limit.

All other snapshot02-to03 changes were reviewed as formatting, including trailing commas and use ordering. Snapshot03-to04 changes only the one test. Both exact 15-file patches apply to their supplied bases and reproduce their proposed hashes. Required caller fields, F/D/step geometry, original limits, synchronous Pending custody, typed primary-plus-first-close error preservation, and cancellation/no-publication behavior are unchanged from the earlier review.

Assembly's immutable native02 evidence records five successful drained commands and 37 passed/0 failed/5 filtered tests, including both allocation retirement checks, actual promise overflow, uncertain initial error/unwind registry retention, and full admitted namespace recensus. Before/after supplied inventories have 1,727 equal entries; final copied native artifacts and reviewed proposal copies were checked locally, accounting for the four explicitly recorded outer-test omissions. This is a bounded native driver, not the integrated server serde, installed MemoryCore, full store suite, native allocation footprint/RSS, physical directory growth, or managed mkdir/rmdir caller qualification. No full-release readiness claim follows.
