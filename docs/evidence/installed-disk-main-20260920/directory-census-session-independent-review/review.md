# Independent census session source review

Verdict: no remaining source-review blocker in immutable review-snapshot-02. Approve this bounded prerequisite for coordinated formatting, compilation and tests. This is not release acceptance, a runtime pass or qualification of filesystem/native allocator growth. The reviewed15-file successor remains unformatted/uncompiled, with10 new census tests and the mandatory configuration serde test unrun.

The initial snapshot had a material blocker: PreparedCensus::drop leaked its resource bundle but discarded Streams and had no registered owner for native close diagnostics. Snapshot02 fixes that by placing Preparing(PreparedResources) inline in the already-admitted registry node before fdopendir. The guard retains that same entry through uncertain close/error/unwind, including actual roots, ancestor locks, both maps, memory provider/leases, configuration and Streams. The original first native close errno and outstanding count remain inspectable. Same-root reopening finds the retained entry before any fresh reservation and returns its native error. There is no mem::forget, detached reaper or post-failure retention allocation in the corrected path.

Ordinary fully closed failures remove the registry node. Existing List::remove deallocates its Box before returning the payload; RegisteredDisk orders its registry lease last, and PreparedResources orders root/descriptors/map backing before its owner lease. Initial successful transition only takes resources after outstanding streams are zero and no close errno exists. It constructs the actual NodeDisk and accepts device promises before committing Installed into the same registration. Errors in this transition retire the local completed resources and remove the temporary registry entry; no incomplete census is installed. The temporary Transition variant cannot be observed by another constructor while the original registry guard is held.

The logical count correction is coherent: checked F+D+R is the capacity of each retained/candidate inode bank, F and nonroot D are independent census checks, and configured roots do not spend D. Full stable-tree readdir work is F+4D+3R including dots and EOF; each synchronous advance performs at most census_work_per_step calls and carries progress forward. Dots/EOF do not occupy map entries. An overlong/repeating scan fails at the checked whole-job bound. The operational one-directory cursor separately uses retained child count plus two returned dot entries, preserving its existing EOF child-count and identity/generation checks.

Pending retains one root iterator, exact current root, preallocated DFS Vec, independent native streams, inactive bank borrow and cumulative counters. The synchronous initial-open registry guard or reconcile State guard spans every Pending step. No bank recreation, third table or cursor reopening occurs at a step boundary. The Vec reserves max_depth before the first native stream; each current name allocation is bounded by max_name_bytes. Existing metadata derivation includes bounded cursor/name workspace and both fixed banks; registry allocation size uses the actual enlarged RegisteredDisk type. This is a logical/requested-layout accounting check, not proof of the existing64KiB native stream allowance or allocator physical/RSS overhead. Configuration byte, handle, depth/name and deadline limits remain unchanged; the new dimensions preserve the original numerical N for each supported generator/fixture.

Each successful fdopendir increments retained stream custody before a fallible handoff. A close consumes the pointer once, records its native errno on failure and retains its slot; cleanup never retries a possibly consumed DIR. Returned read/cancellation errors close every stack stream and reset only the inactive candidate. Unwind invokes the same cleanup while its owner/guard remains live. In reconciliation, failure keeps the old published ledger/counts/promises and marks the owner/device failed; an uncertain stream blocks subsequent pause/reconcile success. Candidate publication occurs only after complete traversal, explicit stream closure, final cancellation and successful shared-promise checks. Banks swap without new allocation; old entries then retire.

Snapshot02 also replaces formatted-only secondary-close context with typed CensusCloseFailure, retaining the original primary anyhow cause and a downcastable exact close errno. The declared diagnostic scope is the first native close errno plus total unretired stream count, not a per-stream multi-error archive. The original Rust panic propagates to its caller; the registry retains the independent native close outcome but does not store an opaque panic payload. This limitation is explicit in DESIGN.md and must not be described as preserving every possible secondary close error identity.

The new tests meaningfully cover the old N=8 counterexample at7 and8 managed files, all F=8/D=8 across bounded steps with stable actual DIR/fd identity, independent cardinality excess, cancellation, incomplete finish, unwind, old ledger/promise retention, no repeat native close, registered initial-close failure through error/unwind, later constructor rejection without new memory attempts, and actual fd retirement before lease credit. Source review is not execution: allocator/native failure hooks and these scenarios still require the owned gates. The three caller migration files exactly match the separately frozen caller package, including required-field and legacy/mixed configuration rejection tests.

No public async scheduling or cancellation responsiveness is supplied by thread::yield_now; locks stay held and synchronous filesystem calls can block. Full managed mkdir/rmdir, remaining listing caller adoption, real directory pre-effect growth qualification, native buffer/allocator bounds, whole-server RAM+reserved fit and a usable2GiB configuration remain open. Those gaps do not disappear because this bounded census algorithm is coherent. No actual Rust source, Cargo target, native program, compiler or debugger was modified/executed during this review.

All15 base/proposed hashes and the pinned patch/files manifest were checked before and after. Target-only git apply-check and exact application reproduced every proposed byte. The review compares snapshot02 to preserved snapshot01; no earlier failure evidence was rewritten. This receipt applies to the exact snapshot02 bytes and requires a final delta check if formatting or later corrections change them.
