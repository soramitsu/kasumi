# Root review: canonical format4 bounded DATA prefix

Verdict: acceptable for coordinated integration and compilation as this scoped implementation, with all full-workspace, crash/fault and bounded-workspace gates still required. No code blocker found in the inspected handoff. This does not close G02 or establish maintenance at the physical cap. Verify the subsequent allocation inventory and changed-test mapping without upgrading its unrun cases.

The single accepted slot/savepoint format is4; UnsupportedFileFormat directly replaces UpgradeRequired. Both current source and proposed14-file hashes were verified. An overlay scan of every crate and vendor Rust file finds no remaining UpgradeRequired, FILE_FORMAT_VERSION1/2/3, PageListKind or SYSTEM_FREED_TABLE use. The obsolete raw name is retained solely for rejection before allocator interpretation or mutation. Historical evidence is not rewritten.

Selection admits at most400 page IDs into one inline native-layout array. Checked records have nonzero count, so it scans no more than400 selected records and one lookahead. It stops on an ineligible horizon row before decoding it, records retained debt, checks an eligible lookahead's canonical shape before any extraction, and remembers original root/horizon/lastkey/record/page totals. Actual extraction rechecks the root and horizon boundary while the existing system namespace/writer guard serializes mutation, removes only through the selected key and explicitly closes the iterator. Fixed push bounds and post-extraction counts reject inconsistency and poison the transaction. This is not zero-allocation extraction: cursor paths, coalescing leaf buffers, current-system frees and COW/allocator/checksum metadata remain separate live owners.

The prepared allocator excludes the fixed deferred DATA slice and borrows the single current-system-free vector after allocator-state table reservation. It does not return either to the live allocator. create_table_and_flush_table_root reserves the master entry before the callback; its final checksum and insert_inplace path allocate no physical pages. Current-system guard remains held through snapshot construction; false retries drop it before removing prepared rows. Winning mem.commit must succeed before either historical DATA or current-system pages are physically freed. The live allocator therefore still protects the prior winning roots through preparation/header/sync failure. Existing retained terminal/disposal and allocator invalidation remain in place.

ReadTransaction and savepoint snapshots retain user roots only. Existing system inspection is serialized by write guard or exclusive construction/integrity paths; this change does not add a public historical system reader. Early header normalization writes are removed from low-level open/reload so canonical namespace checks occur before later successful repair/begin_writable publication. The new rejection tests must actually verify unchanged bytes on writable/read-only failures; static review alone does not qualify file-lock or Drop behavior.

Compaction still has fixed maintenance commits and the existing relocation allowance. Preview counts every unselected DATA row, even one beyond the sampled reader horizon. Thus reader release cannot turn a no-eligible-debt observation into false completion after only one bounded batch. A conservative additional observation is allowed. Existing consuming compact behavior, its error custody and whole-file candidate scan remain distinct open concerns.

Prepared tests preserve the original logical extraction-close failure but necessarily replace the rejected200-ID SYSTEM shape with400-ID DATA records. Changed page geometry must freshly exercise real growth/sync error and preserve original I/O identity; old passing results cannot prove that. The full vendor library, original header/resize/sync/cache/custody faults, savepoint and public integration/crash tests remain mandatory. Duplicate/page-range corruption validation, DATA_ALLOCATED historical work, total COW/workspace/RSS bounds, and reserved physical maintenance capability are explicitly unqualified. No quotas, deadlines or compatibility fallback are introduced.
