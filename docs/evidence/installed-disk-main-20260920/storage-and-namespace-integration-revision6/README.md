# Storage and namespace integration revision 6

Target-only successor to immutable revision 5. Three test-only changes fix two newly reached integration assertions. The archive case explicitly proves reconciliation refuses a live managed directory, retains charge/fence, then drops that owner and reopens through the exact same disk after census. The session case validates the actual fixed terminal container and enclosed original ciphertext without consuming injected durable-read faults, and verifies reserved inode/extent and complete physical bytes survive resolution. All original observer faults, read failures, outcome variants and cleanup assertions remain. No production behavior, limits, fallback, test names or exclusions change. Revision 5 full runtime remains preserved (322 passed, 2 failed, 2 ignored); this successor still requires qualification. Complete production caller migration and total memory bounds remain open.
