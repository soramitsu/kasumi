# Filesystem namespace admission and claim-path correction

This target-only successor is not applied to actual source. All files remain under the requested repository on master. The predecessor `backup-namespace-admission` candidate03 passed its exact 65-test module cohort; its source, native inputs and failed earlier trials remain unchanged.

`claim-path.patch` contains the six-file correction over candidate03. `namespace-session.patch` contains the complete thirteen-file namespace/session successor over `directory-managed-namespace-generic` plus the captured actual backup/audit source base. Both patches were checked and applied to separate copied bases with exact resulting-byte readback. `cumulative.patch` additionally composes all predecessor directory/census primitives and the current session implementation into one 57-file patch over the verified actual source bases. Its actual-source git apply --check passed; it was applied only to a target copy with exact 57-file readback. `cumulative-manifest.json` binds every actual base and proposed hash. Use that cumulative patch for a fresh adoption, or the ordered smaller successors where predecessor changes are already present; do not apply both paths.

The correction removes the unused `slot.relative` String and pre-claim PathBuf. Session UUIDs are lower-hyphenated into a 37-byte stack buffer. `NodeDiskDirectory::claim_descendants` derives the exact binding from the retained directory's configured root and existing names, verifies its enrolled settled prefix, enforces checked depth and name limits, and installs the claim under one State lock without allocating names or opening descriptors. The claim is declared before all owned put paths, admissions and file/directory owners. Rust return/unwind drops those locals before releasing the lane. Returned errors, caller-owned ciphertext and preexisting async workers have separate lifetimes and are explicitly outside this lane's memory claim.

The two new regressions cover allocation-free claim acceptance, contention and nil rejection; exact public returned-error allocation baseline; lane release after an early header error; and exact configured-root/nested binding and name/depth boundaries. The existing classification race, permanent terminal reserve restart, full fixed extent, shortage and ownership tests are retained. Native01 selects 67 tests, with the same five inherited device tests compiled but filtered, and keeps the original 120-second per-phase cap. Native01 passed all 67 selected tests (0 failures, 5 inherited device tests filtered) in 5.36 seconds; production and test compilation were clean. Runner PG56757 and groups PG56760/56763/56772/56776/56777 drained; all 1,817 inputs and executed binary hashes were unchanged. All 65 predecessor test names are present plus the two intended regressions. `qualification-receipt.json` binds the frozen candidate, exact native selection, test names, results and drain receipts. An initial test-name receipt parser missed a geometry test that prints before its success line; the attempt is preserved and the corrected parser accounts for all 67 without rerunning tests.

The standalone harness scope is unchanged: it compiles proposed NodeDisk and filesystem open/put/get/terminal modules and extracts the exact BackupSessionSlot. Authenticated GC methods are retained in proposed source but omitted and hashed in the harness, without fake proof or StorageAccess authority. Full store/constructor/GC compilation, strict lint and runtime integration remain required. Inherited generic file/ordinary directory close custody, full caller/worker/ciphertext/stack/diagnostic admission, all other directory callers, supported-filesystem physical directory bounds and complete allocator/RSS qualification remain open. No release completion or complete drain claim is made.


The independent implementation review found no blocking issue in the frozen six-file claim-path correction and independently checked the exact candidate, apply/readback and native67 evidence. Its receipt is `../backup-namespace-admission-revision4-independent-review/receipt.json` (SHA256 `cf8b3e802ec479ef46a4f33444923148e89427321a58c7b258d02c693bff202e`). This approval does not re-review all 57 cumulative files. Dynamic held-State waiter observation and a full deallocation observer were not added; binding and lexical retirement were reviewed structurally.

`composition-notes.json` identifies the two files requiring explicit composition with the independent `storage-census-adoption-revision3` candidate: store `lib.rs` and `node_disk/tests.rs`. Their actual bases match, but proposed hashes differ. No combined candidate or combined runtime qualification is claimed.
