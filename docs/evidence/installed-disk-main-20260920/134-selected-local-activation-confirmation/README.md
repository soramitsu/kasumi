# Selected voter local activation confirmation

Attempt134 completes the full authority library:62 pass,1 fails. The original activated_target_keeps_operational_suspension_and_membership_across_restart case fails at journal.record_activation line1325 after all follower confirmations. The original leader-bound proof was produced and successfully signed before that loop. Later record_activation calls TargetSigner::sign_activated, whose release re-runs target_activation_observation on the same original Database. Its linearizable barrier reports has-to-forward with no leader, and original authority denial is wrapped as UnknownOutcome. The run does not capture an atomic route/storage cause; it is not marked passing. Terminal source inventory is unchanged and actual group drained without runner timeout/signals.

The fixture now uses the already successful exact signed activation to confirm the selected voter's own persisted application before writing its local projection, as it already does for other voters. This is an added single existing confirm_target_activation call with the same selected TargetOperation and signed activation, no retry or fresh authorization. It asserts the same activation fact and selected observer identity, and uses that local proof for initial projection and exact replay. The original leader-bound proof and its original successful signing remain intact and retained until their original explicit drop. The added local proof is explicitly dropped at that same drain boundary.

This relies on production local_target_activation_observation verifying the exact signature, current phase authority, actual local completion/activation, exact persisted Raft application and membership, and release fence. The local confirmation API never proposes activation or substitutes a new issuer winner. Original independently signed activation, forged follower rejection, every other voter confirmation, suspension checks, exact execution/restart, substituted journal rejection and subsequent ordinary serving maintenance assertions remain. The test no longer assumes the node that originally led remains leader after all voters finish their work. It still cannot succeed through a failed storage owner or invalid/missing local fact because the existing confirmation checks remain mandatory.

No production API, storage format, deadline, workload, election setting, compatibility route or failure assertion is relaxed. Target-only proposal is uncompiled/unapplied and awaits independent review plus original-case runtime verification. The failed complete authority run remains authoritative evidence of its own input, not replaced by this proposal.
