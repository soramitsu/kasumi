Run97 observed the same immediate gate-close Store/Write cause in a real replication stream (owner1,target3), instead of a state-machine worker. Preserve repeated Complete plus exact original DrainIssue Arc, admit only the two already observed complete diagnostic values in a returned Storage variant, require actual child presence, valid fixture target1..4 (voters plus serving learner), nonzero owner, and no snapshot/join/auxiliary/core/incoming failure. Original immediate production fencing unchanged. Added a negative child classifier test; no workload/deadline change. Preparation static only.
