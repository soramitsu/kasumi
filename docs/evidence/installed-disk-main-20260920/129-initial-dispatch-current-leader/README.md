# Run129 initial dispatch current-route fixture correction

Run129's full original lifecycle target ended with 12 passes and two failures. The first failure is `prepare_completion` line253: the read at `next_recovery_dispatch` returned Unavailable. The original diagnostic identifies node1 as healthy (`running_state: Ok(())`), Follower in term5 with no current leader and applied index6. This is the exact database cached at line205 before several prior accepted or rejected recovery control calls. The failing observation occurs before the uncertain activation scenario. Its error is not accepted or classified away.

The fixture now constructs the finite observation context once, then selects the current leader immediately before making that same first-dispatch observation. Operation ID, phase ID, original RecoveryStart and subsequent assertions are unchanged. The existing context's 60-second authorization begins before leader selection, so routing consumes its existing lifetime. Existing leader discovery's 10-second bound, engine read deadlines, fake clock, workload and phase handoff remain unchanged. The single next_recovery_dispatch call still must succeed; there is no failed-call retry, renewed authorization, alternative request identity or fallback to stale local state.

Production `next_recovery_dispatch` first enters actual work and then awaits `lifecycle_barrier(context)`. The fixture's existing `leader()` selects a node advertising itself and requires an actual linearizable barrier. That is appropriate routing for this ordinary observation. It does not pin leadership atomically across the subsequent call, guarantee eventual elections, or excuse a storage/core failure. All original database/resource owners remain in Fixture; only the local routing Arc is refreshed.

The one-file diff is six additions/removal lines around the original first dispatch. The exact prefix before that block and suffix after it are preserved. No production source was changed, and no tests, native programs, compiler, or debugger were run. Static target-base apply checking passed. The second run129 failure at commit_next_intent line1776 has a different original write timeout and is explicitly outside this correction; root owns its diagnostics. Any combined proposal must preserve both independent patches without overwriting source.
