# Engine source fixture migration (prepared only)

Source: master 600c0ca2b2c4c22b89b44ccd932eca02272c70f1. This layer applies after the corrected engine memory guards and Database construction proposal. The manifest identifies the exact intermediate baseline for every overlap. It requires the mandatory store metadata revision 2, MemoryCore disk adapter, root engine fixture helper e3bebad9, and store fixture backend constructor 2c997182. It does not include root-owned test_utils.rs or engine integration tests.

- Real SecurityAudit/Database/TargetJournal/snapshot fixtures admit one exact NodeAdmission before either physical owner is opened. Existing total policies gain only the computed mandatory physical metadata. Fixtures using a fixed 2 GiB memory source preserve its original 256 MiB total directly, without resolving against host RAM. Persistent and scratch roots are siblings. Reopens keep the actual disks/core/facade instead of constructing new governors. Directory fields follow resources in fixture drop order.
- Audit cache and backup destinations use the already admitted persistent root/owner. FaultBackend audit uncertainty retains its original backend and injected sync failure, using open_fixture_backend_on_disk to retain the real governor binding without fabricating a physical file claim.
- The shared worker-permit test preplans both actual installations against one original operation allowance and one facade. The snapshot source/target pair does the same for the original 80 MiB allowance or canonical Default total. Capacity rejection uses a retained reservation on that same actual core, leaving exactly materialization workspace minus one byte or 1 MiB. Deadlines, row counts, limits, failure categories and healthy retry checks remain unchanged.
- Startup cancellation and audit drain assertions include the exact process-retained installed metadata, while still proving operation/owner charge release and original issue identity. The cancelled constructor's facade Arc held by FixtureStorage is explicitly dropped before the weak-facade check.
- Pure codec tests use explicit bounded TestDiskMemory (64 MiB, 32 reservations), never for a real NodeAdmission-coupled fixture. ScratchScope requires that explicit governor and retains its private directory. Helpers returning an image/view receive an existing disk rather than creating a short-lived directory underneath their result.
- Fixture apply_command now requires &Arc<ScratchDisk>. Private ApplyScope::Committed keeps production application behavior, while ApplyScope::Fixture carries the caller's actual disk into permanent receipt/terminal owner construction. No compatibility overload, lazy owner or new governor is installed in the application path.

39 changed paths parse through rustfmt stdin. The patch applies to the exact proposed intermediate input tree, and every resulting SHA matches manifest.json. No Rust build or test has run for this layer. The source worktree remains untouched. Independent peer review covered both snapshot public API capacity cases and the real cancelled-startup census case with no actionable finding.

Required validation after coordinated stack assembly: workspace check all targets/features; engine library suite (including real audit/worker/startup cancellation, bootstrap/reopen, credential reducer, snapshot validation and memory denial cases); strict workspace Clippy and formatting. The known 512-row staging/deadline issue still requires admitted batching and is not resolved or qualified by this fixture migration. The ignored 100,000-receipt workload and native production-capacity/platform qualifications remain separate explicit gates.
