# Root fixed-cache revision 2 review

Reviewed the full first revision, the complete revision1-to-revision2 delta, the independent review and the separate clean-close assertions. Applied revision2 3f2548f12aad8c27f72a8645162296ec85079d9cdc9a2adf6363f5c9a1e4bcdc with follow-up b71cc8d16ed8fef78a14426e41cfdc8ffc2582894d0d17c67b5b5e37a6879dc2 only after all current base hashes matched. Root application receipt is 121-cache-applied.json. This review is static; runtime evidence has separate numbered receipts.

Revision1 was not applied: the A-borrowed/B-second-chance ordering could repeatedly remove and reinsert A, flush zero bytes and incorrectly admit another entry to a full stripe. Revision2 scans eligibility in the existing queue, gives eligible pages one second chance, and independently rechecks actual map length under the original lock. The new real cache regression observes the required B flush, exact cardinality, held A payload and final data.

Capacity refusal precedes cache invalidation, payload allocation and new-key registration. A selected page denied by cache admission is returned to its exact buddy allocator under the original state lock; any previously admitted file growth remains owned until actual abort. Original eviction I/O remains the original I/O error and fences the physical owner. The separate cache latch prevents an ignored operation error from becoming a successful commit. Retained terminal and rollback cells keep their original errors and panic outcomes. Positive disposal uses the matching retained database witness added by disposal2933, and the transaction guard remains last.

The required follow-up explicitly checks both database close phases return Ok; Settled alone would permit a shutdown error. The test still checks original cache-error pointer identity afterward. No static blocker remains for this prerequisite. Metadata cardinality is not a total payload, arbitrary-reader, transaction, allocator or RSS proof. Production NodeDatabase adoption and the complete workspace bounds remain open.
