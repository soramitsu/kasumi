# Generic directory acquisition correction

**Target only, unapplied.** This distinct correction preserves the frozen `directory-managed-namespace` package and its failures/native47 pass. Root review found that the first package newly added `RetainedParent::prepare` and `parent.verify` to generic nonroot opens/observations. Those new descriptor paths were outside the retained operation and could not be classified as inherited behavior. This successor resolves that specific finding.

Nonroot `NodeDisk::open_directory` now enters the same PendingDirectory protocol before its first new native descriptor. It allocates bounded names, ancestry and actual private Arc backing under the existing admitted envelope, counts the operation slot, and traverses the exact enrolled root/parent chain with State-owned current/next descriptors. Each observed ancestor must match its typed directory record, full binding, parent identity, mode, length and extent. Ancestry is filled into already-allocated backing; no binding-index scan or third map is introduced. After descent, the actual parent descriptor is transferred directly from the walk and registered before the final child opens. All walk closes are explicitly observed; uncertain failure retains the remaining physical resources and original diagnosis. Healthy unknown final absence retires concrete resources/backing before returning the slot.

The additional `parent.verify` traversal in ordinary observation has been removed. The existing full enrolled walk remains, and the retained parent descriptor's metadata identity is checked without acquiring another descriptor. The unchanged configured-root acquisition and existing ordinary owner Drop/RetainedParent retirement still follow their prior close contract. **This correction does not establish complete directory-drain/native-close qualification for those remaining inherited paths.**

`successor.patch` contains four files over the frozen managed package; the cumulative patch contains 51 files over the earlier actual baseline. Saved-baseline application checks reproduce every exact proposed hash. No actual checkout source, branch or worktree was changed.

Native-01 compiles exact production/test module bytes and runs the complete prior 47 tests plus two new tests for generic nonroot acquisition and prepublication close-failure custody: **49 pass, 0 fail, 5 inherited device tests filtered**, 2.29 seconds. There are no compiler warnings. All five groups (14226, 14227, 14239, 14257, 14261) drained; all 1,731 inventoried inputs and actual executed binaries are unchanged. Original 120-second limits remain. Result SHA-256 is `62f101ddd9e9bd37ed11d8b7bdc48f3d8717b3b1e69d681b83e0f674fcce0af5`.

The same explicit external test exclusions remain; this is not a workspace/server/installed provider qualification. The fixed terminal-session admission primitive and 44-site caller adoption, supported-filesystem pre-effect growth bounds, native allocator/FD/RSS and complete 2 GiB fit remain open. No compatibility decoder/alias, raw adoption fallback, production quota increase or shortened acceptance workload is introduced.
