# Independent fixed-cache revision 2 review

Verdict: no production blocker found in static review, provided the separately frozen clean-close follow-up is applied with revision 2. This is not compilation, test execution, a measured total workspace bound, or release qualification.

Reviewed cache.patch SHA 3f2548f12aad8c27f72a8645162296ec85079d9cdc9a2adf6363f5c9a1e4bcdc and manifest SHA 562bc4b6aadafd07dac3168b5793d460557afd264ffd74c03fb2e5f12e593f73. All nine proposed hashes and their exact current-source bases match; the new test file is absent from actual source. git apply --check passes. Retained transaction and transaction-field baselines exactly include applied disposal2933, and the new delta preserves the mandatory matching database witness, Option custody, first-outcome cells, and guard-last field order.

The revision1 mixed-selection defect is corrected. LRUWriteCache now uses an eligibility-aware selector instead of removing and reinserting borrowed entries. Two bounded passes visit the fixed queue registrations, rotate ineligible entries without clearing their flags, and give eligible entries one second chance before selecting them. The actual stripe mutex excludes concurrent slot insertion or guard-return changes during selection. The all-borrowed admission test returns before this selector, preserving queue and flags as well as payload/counters. After a successful flush, prepare_write_entry independently checks the actual map length before admitting insertion; a zero-byte flush cannot authorize a full stripe.

The exact mixed regression is nonvacuous: hold A, attempt an empty flush, write/return B so it has a second chance, then insert C. Revision1 can repeatedly remove A and return zero flushed bytes while B remains available; it then inserts a third entry. Revision2 rotates A and selects B, and the test requires exactly one backend write, two live entries, unchanged buffered-byte total, no denial latch, A's untouched borrowed payload, and all three correct payloads after flush/readback. The direct LRU test separately checks a bounded all-borrowed scan and one queue registration per live key.

All three read-insertion paths perform entry-capacity preflight under the appropriate read-cache write lock. The only new write-key insertion follows prepare_write_entry while retaining its stripe lock. A failed physical eviction reinserts the same existing buffer, returns the original io::Error and owner fence before any CacheCapacityDenied classification, and leaves its byte accounting retained. A successful eviction is actual data write before accounting decreases. This establishes metadata cardinality behavior, not release of externally retained page payload or a mandatory memory lease.

The selected-page rollback is limited to CacheCapacityDenied from preflight. The candidate has already been selected (and may follow actual admitted, synced growth), but has not yet entered PageTracker or UncommittedPages: both callers register only after allocate_helper returns Ok. The refusal branch removes its debug allocation registration, returns exactly that buddy allocation under the original allocator lock, and updates the region tracker at the merged order. It keeps the actual expanded layout/file. The original abort performs the existing rollback and physical settlement; no disk credit is released merely because a cache slot was denied. Other storage errors preserve the failure path and are not reclassified.

The distinct denial latch persists through the transaction and prevents later allocation/commit success. Retained commit records the original cache refusal in its terminal cell and the actual rollback result or panic separately. Only a positively settled transaction can be disposed using the matching retained database. The positive cache fixture preserves the original error pointer through actual writer reuse and database close. The two uncertain cases keep the actual transaction, database and original file in fixed test custody; they do not claim cleanup from Drop. The separate consuming-API test checks dependency behavior and is not a production compatibility/admission alternative.

Required test correction: revision2's positive fixture initially replaced consuming close().unwrap() with only DatabaseCloseSettlement::Settled. That would accept a returned shutdown error when backend close succeeds. Reviewed the separate correction.patch SHA b71cc8d16ed8fef78a14426e41cfdc8ffc2582894d0d17c67b5b5e37a6879dc2, manifest SHA 411000344b1d6e825744be87fcc95ed6589686d952d6c9d8442d55662a2a1117: it asserts both shutdown and backend are Returned(Ok(())) and still checks the original cache-error pointer afterward. Its before bytes exactly match revision2's proposed test file. Apply it as a required part of the reviewed result.

Remaining boundaries are explicitly documented by the author: the depth allowance is a selected metadata policy rather than a proof that arbitrary external guards fit; payload/cache high-water and total transaction workspace still require the restricted staging proof and admission; production callers still need direct retained aggregate adoption. No memory coefficient, added capacity, stack adjustment, test-deadline extension, or fallback compatibility behavior was inferred by this review.
