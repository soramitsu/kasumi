# Retire the superseded routing Arc before shadowing

Attempt137 runs both original uncertain-activation cases but each fails at the first reopen: lifecycle.rs222 open_existing returns operation-would-block from the actual exclusive NodeFile lock. Stack traces place the failure at prepare_completion342 after the original snapshot/drop(db)/close. Source and exact binary remain unchanged and the owned process group drains in22.661 seconds without outer timeout or signals. Neither case reaches the original129 phase failures or the new CommitIntent diagnostic.

The new current-route binding at253 shadows the original db selected at205. Rust keeps that first Arc alive until the containing preparation scope ends; drop(db)340 drops only the replacement binding. This newly introduced stale alias retains the original database/file across reopen. Explicitly drop the old routing Arc immediately before binding the new one, after capturing the same finite context. Fixture still owns all actual databases and storage during route selection. The replacement Arc follows the original explicit drop before close/reopen. No file-lock retry, hidden owner removal, new deadline, authorization renewal or changed shutdown contract is used.

The later shadow around661 retains its predecessor only until prepare_completion returns, before the separate continuation later closes the fixture; no earlier same-scope reopen follows that shadow. Its case is distinct and unchanged. Original failures and review/application evidence remain immutable. This target correction is exactly one added drop statement and is independently reviewable.
