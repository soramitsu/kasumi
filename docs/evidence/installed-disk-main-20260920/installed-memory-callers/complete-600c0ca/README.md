# Complete server installed-memory caller proposal

**Proposed, unapplied, uncompiled. First-release direct replacement. Main/master only.**

`server.patch` is a combined replacement for the five earlier server proposal layers, based on master `600c0ca2b2c4c22b89b44ccd932eca02272c70f1`. Do not apply those earlier layers in addition to this patch. `manifest.json` records every exact old/new file hash and the patch hash. `baseline.json` preserves the complete original server inventory and five layer inputs; the original layer checkpoint remains in `../checkpoints/before-complete-600c0ca`.

Required companion proposals: canonical store metadata revision 2 (`10ebc1c6...`), mandatory MemoryCore store-admission adapter, exact-store/core guards and private Database construction (`05d51188...`), and root engine fixture helper (`e3bebad9...`). The helper's snapshot/encoding APIs require the fixture's existing scratch owner.

Production entry points select installed MemoryCore policy without a cfg-test bypass. Internal constructors receive mandatory RuntimeStorage, validate the exact policy, and create a fresh runtime facade before physical storage. Only new-installation/example writers select the approved explicit 2 GiB policy; existing installed configuration is unchanged. Fixture entry points explicitly select isolated physical registries with an independent core; reopen uses the same configured disk and scratch owners. These are required arguments, not optional ownership or compatibility fallbacks.

Existing fixture policies use canonical resolved original TOTAL plus only the new exact disk metadata leases. No original bookkeeping is added twice, no RSS ceiling is raised, and no fresh-device allowance is added for reused owners. The shared replicated-runtime planner preserves each original data/issuer audit/request payload ceiling, counts one shared core, its actual maximum live facades, and physical owners once; see the prior `shared-cluster-design.md`. Its replacement ownership test retains a sealed old facade temporarily and does not claim full payload capacity during that extra observer lifetime.

New low-level fixture migration uses the root FixtureStorage helper: explicit private persistent/scratch siblings, caller-owned enclosing directories, admitted facade before disks, same core for SecurityAudit/request/snapshot owners, and positive NodeStore closes before physical reopen. Related API restore and backup files reuse the already-planned physical owner. TLS/serving cancellation fixtures still assert that pending real children retain the node and prevent reopen; the released child closes the concrete node before successful drain/reopen. The wrong-installation UUID recovery test models stopped-installation external replacement: all resources positively close, `open_files == 0`, then pause/reconcile precedes both substituted and restored starts. It does not permit a live owner to heal an uncertain namespace.

Validation performed: rustfmt via stdin for every changed proposed Rust file, `git apply --check`, baseline/current byte hashes, and source-call inventory. No Cargo, Rust compilation, test execution, source edits, commits, branches, or external worktrees were used. Runtime success is not claimed. Coordinated workspace compilation/lint and affected server tests remain required, especially the stopped replacement census, TLS/serving drains, signer reopens, shared-cluster accounting, native API lineage, and authority transports.

Still separate: production StartupContext resource-plan integration before future boxing/spawn; complete admitted startup registry/report custody; directory/metadata I/O accounting gaps recorded by their owning workstreams. RuntimeStorage is reusable scalar policy plus exact core/factory, not a substitute for a fresh per-open startup scope. This proposal does not close those release requirements.
