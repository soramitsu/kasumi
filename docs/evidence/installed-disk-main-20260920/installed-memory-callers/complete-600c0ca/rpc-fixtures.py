from pathlib import Path
r=Path('target/installed-disk-validation/installed-memory-callers/complete-600c0ca/proposed/crates/kasumi-server/src')
def rep(s,a,b):
 assert a in s,a[:100];return s.replace(a,b)
p=r/'rpc_authority_tests.rs';s=p.read_text()
s=rep(s,'    let audit_node = NodeStore::create_new_fixture(\n        dir.path().join("audit.redb"),\n        kasumi_store::test_utils::NODE_STORE_ID,\n        kasumi_store::ScratchDisk::fixture(),\n    )','    let audit_physical = crate::runtime_storage_fixtures::physical(&dir.path().join("audit"), Default::default()).unwrap();\n    let audit_node = audit_physical.create_new(\n        dir.path().join("audit/persistent/audit.redb"),\n        kasumi_store::test_utils::NODE_STORE_ID,\n    )')
s=rep(s,'        audit_node,','        audit_node.clone(),')
s=s.replace('        kasumi_engine::admission::NodeAdmission::new(Default::default()).unwrap(),','        audit_physical.admission.clone(),',1) # ONLY first at indent8; deeper separate
# actual signer initialize and reopen share exact context/core
s=rep(s,'            InitializeSignerVerifier {','            let mut input = InitializeSignerVerifier {')
s=rep(s,'                initial_certificates: vec![certificate.clone()],\n            }\n            .initialize()\n            .await\n            .unwrap();','                initial_certificates: vec![certificate.clone()],\n            };\n            let storage = crate::runtime_memory::RuntimeStorage::isolated_fixture(&input.admission, &input.persistent_disk, &input.scratch_disk).unwrap();\n            input.admission = storage.policy().clone();\n            input.initialize_with_storage(storage.clone()).await.unwrap();\n            let admission = storage.facade().unwrap();')
s=rep(s,'                    crate::persistent_disk::open(&persistent_disk).unwrap(),\n                    kasumi_store::ScratchDisk::open(kasumi_store::ScratchDiskConfig {\n                        directory: directory.join("scratch"),\n                        max_bytes: 64 << 30,\n                        min_free_bytes: 256 << 20,\n                    })\n                    .unwrap(),\n                    kasumi_engine::admission::NodeAdmission::new(Default::default()).unwrap(),','                    storage.open_persistent(&persistent_disk).unwrap(),\n                    storage.open_scratch(&input.scratch_disk).unwrap(),\n                    admission,')
s=rep(s,'        let store = TenantStore::initialize_catalog(\n            NodeStore::create_new_fixture(\n                dir.path().join(format!("verifier-{node_id}.redb")),\n                kasumi_store::test_utils::NODE_STORE_ID,\n                kasumi_store::ScratchDisk::fixture(),\n            )\n            .unwrap(),','        let verifier_root = dir.path().join(format!("verifier-{node_id}"));\n        let physical = crate::runtime_storage_fixtures::physical(&verifier_root, Default::default()).unwrap();\n        let node = physical.create_new(verifier_root.join("persistent/verifier.redb"), kasumi_store::test_utils::NODE_STORE_ID).unwrap();\n        physical_nodes.push(node.clone());\n        let store = TenantStore::initialize_catalog(\n            node,')
s=rep(s,'    let mut verifier_stores = Vec::new();','    let mut verifier_stores = Vec::new();\n    let mut physical_nodes = Vec::new();')
s=rep(s,'        let node = NodeStore::create_new_fixture(\n            dir.path().join(format!("authority-{id}.redb")),\n            kasumi_store::test_utils::NODE_STORE_ID,\n            kasumi_store::ScratchDisk::fixture(),\n        )','        let authority_root = dir.path().join(format!("authority-{id}"));\n        let physical = crate::runtime_storage_fixtures::physical(&authority_root, Default::default()).unwrap();\n        let node = physical.create_new(\n            authority_root.join("persistent/authority.redb"),\n            kasumi_store::test_utils::NODE_STORE_ID,\n        )')
s=rep(s,'        let storage = TenantStorageSet::initialize_catalogs(\n            node,','        physical_nodes.push(node.clone());\n        let storage = TenantStorageSet::initialize_catalogs(\n            node,')
s=rep(s,'                &kasumi_engine::admission::NodeAdmission::new(Default::default()).unwrap(),','                &physical.admission,')
s=rep(s,'            kasumi_raft::SnapshotBufferOwner::fixture(),','            physical.admission.snapshot_buffer_owner().unwrap(),')
s=rep(s,'    for installed in installed_verifiers {\n        installed.shutdown().await.unwrap();\n    }','    for installed in installed_verifiers {\n        installed.shutdown().await.unwrap();\n    }\n    audit_node.shutdown().await.unwrap();\n    for node in physical_nodes { node.shutdown().await.unwrap(); }')
s=s.replace('    NodeStore, StorageAccess,','    StorageAccess,');p.write_text(s)
p=r/'rpc_lifecycle_tests.rs';s=p.read_text()
s=rep(s,'    let audit_store = TenantStore::initialize_catalog(\n        NodeStore::create_new_fixture(\n            directory.path().join("audit.redb"),\n            kasumi_store::test_utils::NODE_STORE_ID,\n            kasumi_store::ScratchDisk::fixture(),\n        )\n        .unwrap(),','    let physical = crate::runtime_storage_fixtures::physical(&directory.path().join("control"), Default::default()).unwrap();\n    let audit_node = physical.create_new(directory.path().join("control/persistent/audit.redb"), kasumi_store::test_utils::NODE_STORE_ID).unwrap();\n    let mut physical_nodes = vec![audit_node.clone()];\n    let audit_store = TenantStore::initialize_catalog(\n        audit_node,')
s=s.replace('        kasumi_engine::admission::NodeAdmission::new(Default::default()).unwrap(),','        physical.admission.clone(),',1)
s=rep(s,'        let stores = TenantStorageSet::initialize_catalogs(\n            NodeStore::create_new_fixture(\n                directory.path().join(format!("issuer-{id}.redb")),\n                kasumi_store::test_utils::NODE_STORE_ID,\n                kasumi_store::ScratchDisk::fixture(),\n            )\n            .unwrap(),','        let issuer_root = directory.path().join(format!("issuer-{id}"));\n        let issuer_physical = crate::runtime_storage_fixtures::physical(&issuer_root, Default::default()).unwrap();\n        let node = issuer_physical.create_new(issuer_root.join("persistent/issuer.redb"), kasumi_store::test_utils::NODE_STORE_ID).unwrap();\n        physical_nodes.push(node.clone());\n        let stores = TenantStorageSet::initialize_catalogs(\n            node,')
s=rep(s,'                &kasumi_engine::admission::NodeAdmission::new(Default::default()).unwrap(),','                &issuer_physical.admission,')
s=rep(s,'            kasumi_raft::SnapshotBufferOwner::fixture(),','            issuer_physical.admission.snapshot_buffer_owner().unwrap(),')
s=rep(s,'        let stores = TenantStorageSet::initialize_catalogs(\n            NodeStore::create_new_fixture(\n                directory.path().join(format!("node-{id}.redb")),\n                kasumi_store::test_utils::NODE_STORE_ID,\n                kasumi_store::ScratchDisk::fixture(),\n            )\n            .unwrap(),','        let node = physical.create_new(directory.path().join(format!("control/persistent/node-{id}.redb")), kasumi_store::test_utils::NODE_STORE_ID).unwrap();\n        physical_nodes.push(node.clone());\n        let stores = TenantStorageSet::initialize_catalogs(\n            node,')
s=rep(s,'    audit_store.shutdown().await.unwrap();','    audit_store.shutdown().await.unwrap();\n    for node in physical_nodes { node.shutdown().await.unwrap(); }')
s=s.replace('    NodeStore, StorageAccess,','    StorageAccess,');p.write_text(s)
p=r/'authority_maintenance_tls_tests.rs';s=p.read_text()
s=rep(s,'    let mut stores = Vec::new();','    let mut stores = Vec::new();\n    let mut physical_nodes = Vec::new();')
s=rep(s,'        let store = TenantStorageSet::initialize_catalogs(\n            NodeStore::create_new_fixture(\n                dir.path().join(format!("node-{id}.redb")),\n                kasumi_store::test_utils::NODE_STORE_ID,\n                kasumi_store::ScratchDisk::fixture(),\n            )\n            .unwrap(),','        let replica_root = dir.path().join(format!("replica-{id}"));\n        let physical = crate::runtime_storage_fixtures::physical(&replica_root, Default::default()).unwrap();\n        let node = physical.create_new(replica_root.join("persistent/node.redb"), kasumi_store::test_utils::NODE_STORE_ID).unwrap();\n        physical_nodes.push(node.clone());\n        let store = TenantStorageSet::initialize_catalogs(\n            node,')
s=rep(s,'                &kasumi_engine::admission::NodeAdmission::new(Default::default()).unwrap(),','                &physical.admission,')
s=rep(s,'            kasumi_raft::SnapshotBufferOwner::fixture(),','            physical.admission.snapshot_buffer_owner().unwrap(),')
s=rep(s,'    for store in stores {\n        store.shutdown().await.unwrap();\n    }','    for store in stores {\n        store.shutdown().await.unwrap();\n    }\n    for node in physical_nodes { node.shutdown().await.unwrap(); }')
s=s.replace('    NodeStore, StorageAccess,','    StorageAccess,');p.write_text(s)
