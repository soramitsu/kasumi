# Independent five-file integration review

Read-only main/master review at `600c0ca2b2c4c22b89b44ccd932eca02272c70f1` of `engine-integration-store-agent/callers.patch`, SHA `2b89e57670ab195966f8fc76fc6250d57a946e41ee4c5635742bf460c8403ecc`. No source edits or builds.

No accounting or core-binding defect found in the requested scope. Backup fixed-payload assertions subtract one actual installed resident metadata baseline; backup files and fixed-budget restore targets reuse that same physical owner. The additional snapshot owner and proposal registry remain explicit in the restored-state deltas. Independently rooted restore/wrong-key targets use their own exact audit/facade/core. Replica fixtures retain their configured physical owners across reopen. Source and target persistent roots do not overlap their explicitly retained scratch directories.

Deleted-cache restart positively drains the restored database, audit, and physical node, asserts zero managed open files, and reconciles before reopening. A narrow ordering improvement was reported to root/author: call pause immediately after that zero-open-files proof and before the raw cache unlink, rather than immediately after unlink. The fixture has no competing opener, so no observed race or runtime failure is claimed; this makes the stopped-installation mutation boundary explicit.

Formatting, compilation, runtime and source qualification remain separate gates. Existing namespace fault cases not changed by this migration still require their focused regressions.
