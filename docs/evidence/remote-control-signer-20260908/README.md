# Current Control signer publication checkpoint

Source 82a9e3c publishes exact Stage and forward Activation directives to the
installed physical verifier through a current Control leader quorum observation
and an independently verified finite issuer observation. The original clock,
resource, policy and physical registry remain bound across endpoint retry.
Wrong resource, changed physical owner and missing administrative permission
are rejected. The global winner precedes forward activation and restart retains
the encrypted verifier outcome.

All 43 authority tests, the extended actual private-admin TLS fixture, strict
full-workspace Clippy, fixture-free server checks and formatting pass. Exact
source, lockfile, binaries and original failing/intermediate logs are retained.
The final native fixture passes after the bounded authorization enum correction.

This is a current-leader publication checkpoint. Follower authorization, global
acknowledgment coverage, revocation and complete issuer drains remain open. The
preexisting issuer-local StopStage path still needs global winner/abort binding;
the new remote interface rejects StopStage and retirement. No final rotation or
release acceptance is claimed.
