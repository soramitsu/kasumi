# Control recovery preparation and stop evidence

Checkpoint `f3c0185` passes two replicated journal cases, one actual mTLS native
Control/issuer case, fifteen snapshot cases, two original target deadline cases,
three authorization cases, strict workspace Clippy, fixture-free server checks
and formatting. The native test uses separate real replicated Control and issuer
groups and intentionally retains an unavailable target dispatch. Full signed
three-voter cleanup journal tests use explicit target cryptographic fixtures.

Initialization and later completion/source fencing/activation/route phases are
still unavailable. Fresh admission after expired unresolved non-target dispatch
is incomplete. This checkpoint does not prove complete source-unavailable
recovery, final Linux gates, 3 GiB capacity or endurance. All retained fixture
and integration failures are included with their original dispositions.

Combined integration `40e7154` subsequently passes an all-workspace, all-features,
all-targets check (92 seconds).
