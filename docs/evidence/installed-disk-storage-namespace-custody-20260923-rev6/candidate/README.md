Target-only combined storage, namespace, file custody and explicit-close successor, revision6.

Preserves every frozen earlier composition and native trial. Exactly three paths differ from revision5: two existing test corrections and a cfg(test) read-only NodeFile custody accessor. All338 test names remain, without removed cases or new ignores. Fourteen focused cases (both revision5 failures plus the previous twelve) precede strict full-workspace lint/check and unfiltered full-store gates under the original1200-second cohort bound. A separate final formatting gate follows only native success.

The cumulative75-file source applies exactly to independently copied current-master bases. No actual source edits. Production retained-opening/census caller migration, verified failed-owner release/recovery, and complete memory/RSS bounds remain open. Existing NodeDatabase Complete reports do not establish physical FileOwner drain; see integration-gaps.json.
