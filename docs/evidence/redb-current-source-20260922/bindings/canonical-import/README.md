# Canonical-only redb follow-up

This is a separate first-release change above admission checkpoint
`8fa529231e474e49572691028050fbe9477f4e2b`. It removes the inherited alternate
composite-type matches and the one-phase header/recovery protocol. Current writers
continue to emit transaction slot version 3, the required two-phase bit, and exact
type identities using tags 1, 2, and 4. No migration, decoder alias, secondary-root
rollback, or compatibility API remains in these paths. Bytes identical to the
canonical representation need no producer-specific decoding.

The renamed canonical-format test suite retains the two current composite
collision counterexamples and rejects old composite identities, one-phase files,
and one-phase files with persistent savepoints. Internal header tests verify a
corrupted winner cannot be replaced with an older root, a torn unused prepared
slot is not laundered, and canonical repair cancellation still runs its callback.

## Preserved development attempts

`01-lib-tests.log` failed 3 of 126 unit tests because those tests fabricated the
removed one-phase repair protocol. The final cases test canonical repair or
explicit rejection instead. `02-canonical-tests.log` completed the corrected
128-case unit suite, then failed 2 of 4 file rejection cases: clean upstream close
can emit an identical canonical two-phase header. The counterexamples now clear
the protocol bit deliberately when testing rejection, and separately test that
canonical headers never restore obsolete table aliases. These attempts remain
unchanged; no ignored tests replace their required behavior.

`03` and `04` contain focused canonical-format and strict-Clippy passes.
`check-results.json` records seven checks in `05` through `11`, including a
completed fresh 430-case all-features test run (424 unit/integration cases and six
doctests), default and all-features strict Clippy, host no-std configuration check,
formatting, and 19 companion derive cases plus strict Clippy. `runtime-inputs.json`
records 76 source/build-input hashes; all were verified unchanged on continuation.
The original command records lack complete executable/process-group custody;
the separate owned rerun below supplies that evidence without rewriting them.

`12` through `14` preserve unsuccessful required `just test`, `just test_all`, and
`just fuzz_ci` attempts: `just` is unavailable. This does not pass the upstream
container, dependency-audit, or fuzz workflows. The no-std check targets the host
with panic-abort, not the upstream thumbv7em target workflow.

## Frozen owned rerun

`validate_leaf.py` inventories every vendored file before dispatch and after each
gate, preserves the exact Rust test executables from Cargo artifact records, and
uses the existing process-group custody helper with fixed original deadlines.
Its test roster explicitly retains all 430 successful names from the earlier
broad suite and rejects a missing case, failure, ignored case, changed source,
undrained process group, or missing test executable. Rust tools and Python are
identified by exact paths and SHA-256 digests. The external binary evidence is
retained under `/Users/mtakemiya/dev/kasumi-release-evidence/redb-canonical-owned-20260920`.
Final results are recorded in the `owned-*` evidence files copied after completion.

`provenance.json` and `published-to-fork.patch` were generated by the checksum-
verifying capture script against the original published crate and its exact
upstream companion-derive archive. They inventory 97 fork files. Licenses remain
unchanged. This component evidence does not establish installed Kasumi G02,
cross-platform qualification, release packaging, or authorization to ship.
