Root review approves this four-file prerequisite for scoped integration and original vendor tests. The bitmap and buddy offset helpers validate the count, metadata arithmetic, every child length, and every absolute u32 end. RegionTracker instead stores individual u32 child lengths, so its aggregate correctly uses usize rather than a new u32 cap. U64GroupedBitmap measures logical used words, not retained capacity, and rejects missing backing. Buddy requires the exact max_order+1 bitmap count. reserve_allocator_state checks actual region count and all encoded lengths before any allocator-tree insert, preserving invalid geometry as existing typed corruption. This preflight still owns the original per-region Vec<usize>; its error construction allocates the original typed corruption message.

Every to_vec and from_bytes body is byte-identical to its base. Seven prepared tests inspect accepted boundaries, all supported buddy orders, actual mutations, preserved serialized bytes, invalid backing and virtual overflow without giant fixtures; allocator-count observation wraps only the new helper. Tests remain unrun. The source review does not treat these helpers as complete semantic validation of every corrupt serialized bitmap or as an allocation-free reserve_allocator_state call. The one per-region inventory, insertion buffers, full allocator copies, COW metadata and physical maintenance reserve remain open. No compatibility path, numerical resource cap or deadline change is introduced.
