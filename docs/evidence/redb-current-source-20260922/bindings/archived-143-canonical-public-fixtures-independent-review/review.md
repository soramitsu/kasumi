# Canonical public fixture correction review

No blocker found in the exact one-file correction. The new typed UnsupportedFileFormat(3) checks match current Header rejection before old table decoding for both writable and read-only opens. The actual upstream redb2_6 format3 producer and its committed composite payload remain unchanged. Each rejected open must preserve every file byte. The existing format2/3 single-phase and persistent-savepoint rejection cases remain, with an additional unchanged-byte assertion for the latter. Both current-format public composite mismatch directions and the private stored-legacy-name decoder rejection remain separate coverage; no compatibility path is added.

The original test's successful format3 open was inconsistent with the mandatory format4 first-release contract. Replacing that expectation with exact format rejection is justified by the production protocol change and retains table identity tests at the layer where they can now run. Target apply/check reproduces the proposed hash. The original target formatter dependency failure is preserved by the author's package; this reviewer did not rerun formatting, compilation, Cargo or tests and made no actual source changes.
