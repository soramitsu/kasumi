# Production network driver feature isolation

The network driver now uses the native client protocol without pulling in server, engine, store or fixture code. Historical embedded/loopback features must be enabled explicitly and their matrix is labeled as fixture evidence. Frozen release gates include the network dependency graph and production build, and reject actual compiled fixture features or substituted test binaries.

Scoped tests, strict checks and19Python checks pass as recorded. Combined b3c2c45 workspace check passes after route, staged-budget and issuer-winner integration. This evidence contains no live capacity or final release build result.
