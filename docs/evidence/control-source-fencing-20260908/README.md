# Independently authenticated source fencing

Source `bf9f2401ae0dc421c7f479d48ae86696dcfc66d3` coordinates planned source
retirement through the installed application client, then verifies the original
permanent receipt through its separately resource-bound custody client. The
source-unavailable path instead dispatches the exact durable issuer Fence.
The required nullable source_custody configuration never substitutes a source
application token for custody authority.

The final engine-source lifecycle suite passed seven tests; final strict
workspace Clippy and the actual TLS backup/retirement helper test passed. Raw
logs, source inventory, exact binary hashes and each earlier check's source
limitations are preserved. The earlier production-only check predates the
custody split; its final no-default-features check remains pending. Linker and
lint diagnostics are retained. Later work must create the retirement deadline
when that phase is prepared, and complete forward activation, local confirmation,
route publication and all source-absent/crash release gates.
