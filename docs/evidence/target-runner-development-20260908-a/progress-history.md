# Kasumi target runner progress (2026-09-07)

Verified and frozen control/issuer foundation: /tmp/kasumi-control-lifecycle CLEAN d9ef813fe13723e9020d53cae1a91265c6b88ae3 (descends c30437 credential/lineage). Full B358pass0fail2externalignored,199unchangedinputs,production+strictClippy/fmt PASS. Failed A355/1/2 preserved. Parent/platform notified. Original /Users/mtakemiya/dev/kasumi still5174; do not edit/FF without parent coordination.

ACTIVE worktree /tmp/kasumi-target-runner branch codex/kasumi-target-runner basedd9ef813; private target /tmp/kasumi-custody-substrate-target Rust1.97.1 jobs1 offline. No original/shared target use. Full task remains physical target materialization/initialize/complete/activate/localstop with native SDK. Do not stop after this draft.

Current referenced code (uncommitted):
- AuthorityAction::ActivateCommitted {fence_id,fence_digest,target,control:CommittedActivation {reference,intent_sha256}}. ActivateTargetInput defines stable exact phase input hash. Current raw Activate denied when manifest.lifecycle_controls nonempty. Exact stored accepted Activate intent/open epoch/original cap/source epoch checked in ordered reducer; requires full independent source drain via existing service. AuthorityReceipt now REQUIRED admitted_at_ms actual captured trusted admission; stopped-original receipt retains actual stop admission, not original requested time. Snapshot helper validates successful committed activation original intent/cap and stop ordering. Still needs actual target completion attestation before declaring readiness; current compiled primitive does NOT prove physical target prepared.
- New LifecycleGate in kasumi-serving retains original Control RequestContext, actual opaque pre-request issuer lease and UTC floor. Renewal requires same original exact intent/boot, previous actual deadline still live; expiry permanently closes captured fences.
- StorageAccess::restore_preparation(serving_gate,lifecycle_gate) requires exact current target phase/identity/checkpoint. Ordinary serving() refuses raw prepared gate when lifecycle controls enabled. Lifecycle store cannot serve ordinary traffic; same live catalog requires exact same phase gate Arc, later phase must drain/drop/reopen. check_access includes phase grant and actual key lease; no Data context synthesized. StopLocal cannot open application store.
- Full workspace all-target/allfeatures diagnostic check A PASS after those changes.

ACTIVE focused test C: PTY50742 command cargo+1.97.1 test -p kasumi-authority --allfeatures -j1 activation_gate_tests --test-threads1 > /tmp/kasumi-target-activation-tests-c.log. Inspect terminal before edits to tested sources. A2pass1fail (setup Fence legitimate UnknownOutcome); B2pass1fail (setup StopEpoch legitimateUnknownOutcome). Preserved logs. Added bounded exact original fence/intent/stop setup recovery on actual current leader, no native timeout/admission weakening. C running after these fixture fixes. New tests issuer_tests child activation_gate_tests.rs: raw denial+exact concurrent winner+encryptedrestart; stoppedepoch/queuedcap; actual targetstore rawprep/newArc/late renewal denial. Snapshot substitution test currently mutates all receipt admitted_at copies; should also repair dependent active digest to isolate semantic cap validation, and compare accepted outcome sourceepoch+1 in snapshot helper (currently not yet added). Must add exact preparation requirement in committed activation live check (existing raw effect allows absentprep; new phase shouldn't).

Parent design message sent: a committed Activate Control intent authorizes but does not prove target readiness. Planned native target attestation key per approved node, signer only accepts opaque engine actual persisted materialization/current target quorum observation; signed facts bind checkpoint/genesis/incarnation/phase/node. Initialize consumes matching actual materialization observations from3peers; ActivateCommitted must additionally verify actual completed target proof against key bound in target/control intent. No DTO/callback/provider-success flags. Exact new caller key shape not finalized yet; send before verified handoff. Parent has not replied to this newest design message yet.

Remaining architecture from /tmp/kasumi-target-runner-draft/INTEGRATION.md applies:
- typed RestoreAuthorization Data vs opaqueLifecycle; never rewrite sourceData context
- actual phase owned work scope installed before provider construction, independently keyed permanent local journal, same grant through IO/decrypt/CPU/persist/Raft/release
- VerificationWork must retain explicit phase gate and actual reservation/work registration until detached/blocking work really ends; cancellation/expiry bounded
- closed engine target phase envelope can be explicitly prefixed beside raw Command (no compatibility parser) or clean shared type; bind claimedgrantnode to actual appliedRaft leader node, exact original phase cap at ordered execution
- genesis/snapshot bounded permanent phase facts and restore lineage; source immutable rows/hashes preserved
- after source independentFence/full drain + issuer Activate, complete target activation under exact grant then servinglease promotion/reopen; no ordinarytraffic duringphase
- incarnation-wide StopTarget+full drain plus local actual work drain+permanent tombstone beforecleanup; StopActivation insufficient
- terminal metadata recovery after originalphaseexpiry needs fresh actual installed Control/currentquorum admin observation, no app provider or oldgrant revival; a signed historical receipt alone isn't renewable authority.

Off-tree lifecycle_gate.rs draft is now integrated; target_invocation/target_records/typedRestoreAuth drafts still uncompiled/unintegrated and need adaptation to LifecycleGate.

## Updated 2026-09-07 target engine checkpoint in progress
- Original native5174 and clean d9ef813/c30437 trees unchanged. Active /tmp/kasumi-target-runner only; private /tmp/kasumi-custody-substrate-target jobs1.
- Required LifecycleNode.attestation_public_key binds an installed Ed25519 target signer. TargetSigner only accepts private actual materialization/completion proofs, no raw fact signing. Completion signatures include fresh observer node/term/revision separately from immutable original applied leader/term fact, enabling actual restart/current-leader recovery.
- TargetMaterializationConfig explicit node/inc/voters/admission (no unused Raft config). materialize_target_replica performs full backup graph verification under RestoreAuthorization::Lifecycle, never synthesizes Data authority; actual deterministic bootstrap publication+independent custody hash/readback; no Raft startup during materialization.
- TargetOperationScope and original TargetOperation own work, cancellation, absolute deadline, slots. TargetReleaseFence retains exact operation identity/deadline/token/phase without renewable admission. Review found same-gate/new-operation proof release bug; fixed and actual negative passed. Fresh readback must construct a fresh private proof. VerificationWork retains registration/reservation through actual blocking work completion.
- TargetQuorumInput (renamed from early TargetCompletionInput, no alias) exhaustive3 signed actualmaterializations; same hash domain for Initialize/Complete, phase separately bound.
- closed prefixed TargetCommand::Complete verified in ordered apply against signed Control phase/original credential admission/actual applied leader, exact actual3voter membership/endpoints and materializedbootstrap. Bounded target history/audit/accounting; pendingrestore clears but suspended stays. Private VerifiedTargetCompletion reestablishes current target quorum/originaloperation fence at sign/release. Background target proposal retains serialized gate/work/reservation until actual Raft effect finishes even caller cancellation.
- open_target_replica loads only an actual materialized image, verifies complete3native signatures and exact physical genesis hash; persist exact Initialize intent in independent custody before Raft startup. TargetReplica owns group; Drop closes original gate and schedules actualDatabase.shutdown while retaining separate scope group WorkRegistration through shutdown. Worker owns original operation while startup awaits, not canceled by dropping caller. Native registry/localstop/drain still to integrate.
- open_replicated/initialize_replicated now reject lifecycle-gated storage; explicit target paths only.
- Engine alltarget/allfeature diagnostics F PASS (no warnings). G initially one wrong recovery_workspace_bytes argument, fixed before testA.
- New authority devdependency kasumi-engine (Cargo.lock+1) to run actual target engine+issuer integration, no productioncycle.
- /tmp/kasumi-target-materialization-tests-a.log compile failure AuthenticatedNode not Clone preserved; explicit fresh verified node context corrected.
- /tmp/kasumi-target-materialization-tests-b.log PASS2: actual encrypted fullbackup/materialize/replay/noRaft/originaloperation/wrongkey; actual3materializations/init/currentquorumcomplete/exactretry/encryptedrestart/signature substitution. Control signing in these issuer-module fixtures is explicitly signed fixture; real nativeControlquorum path already separately tested foundation, composednative target pending.
- Authority ActivateCommitted now REQUIRED CommittedActivation.completion(Box<SignedTargetCompletion>), ActivateTargetInput.completion_sha256. Ordered issuer validates actual completion signature and exact origin against Activate control intent and installed manifest plus fullsourcefence/drain. Old issuer tests use explicit target-signature fixtures (not proof of physicalready).
- Snapshot validation now re-verifies3 materialization signatures for all completion history.
- /tmp/kasumi-target-activation-tests-e.log 1PASS2FAIL: heavier positive issuercalls saw Unavailable/UnknownOutcome under tiny fixture election windows. Preserve. Updated tests to recover exact immutable original command on actual current leader rather than assert timelyack. ACTIVE F process PTY65624 > /tmp/kasumi-target-activation-tests-f.log, inspect before source edits.

NEXT: finish local target Activate ordered prefix variant, issuer VerifiedActivation accepts ActivateCommitted too; active storage+phase capability path (rename new StorageAccess::restore_preparation to target_phase, required allowactive onlyActivate) and deny rawData/admin Suspend(false)/allrawops before native target activation even with active servinglease. Activation command binds exact actualissuerwinner/completefact and finiteoriginalphase, produces bounded TargetActivationFact and suspendedfalse. A fresh explicitly committed Activate phase may rollforward a prior global winner without changingthatwinner; oldcapturedfences remaindead. Need verified completion enforcement tests actualengine->issuer.
Then required native installed targetconfig/phase acquisition+renewal, currentControl metadata recovery, before-provider permanentlocaljournal, targetstop fullissuerdrain + actuallocalworkersstore shutdown + permanentcleanup tombstone; typednativeSDK. Do not stop after engine tests or checkpoint. Source unavailableDR never uses sourceDataquorum. Healthy path also exactfreshretirement proof. Parent needs boundlineage/dataJWT currentinc allnative regressions.

Platform review dispatch found exactadmit_dispatch tighterdeadline lost by ..p reconstruction; reported, platformacceptedwillfixafterfrozengate. Platform currently reviewing target_state/service/bootstrap_target_quorum read-only if slot available. No externalproduct/source edits.

### Latest terminal results / active process
- ActivationE/F initially misdiagnosed as fixture ack instability. G added actual running_state diagnostics and exposed REAL production committed JSON decoder failure: internally tagged AuthorityAction buffering turns JSON u64 map keys into string Content; nestedtargetmaps deserializefailed on everyreplica. Do not call these simply fixture failures. Preserved E/F/G logs. Added canonical string-decimal u64 key deserializer to required target/lifecycle maps, rejecting duplicate/noncanonical keys (no alternatelegacyformat, encodedbytesunchanged); fulltaggedcommandroundtriptest.
- ActivationH PASS3 /tmp/kasumi-target-activation-tests-h.log after decoder fix.
- Platform found TargetReplica Drop try_current could loseWorkRegistration withoutshutdown ifowner moved to nonTokio thread. Fixed: capture originatingHandle onconstruction and always spawnshutdown withregistrationretained. Actualstd::thread drop test passes.
- LocalActivate is implemented/compiled in target_activation_service.rs and target_state.rs prefix::Activate. TargetOutcome internalenumboxes complete/activate. Verifies actualissuer SignedAuthorityReceipt::ActivateCommitted, exact actualcompletedfact/sourcefence input; originalfiniteControlgrant atserializedpreproposal andactualappliedleader; records immutableTargetActivationFact and suspendedfalse. Requiresnewnativephase authority evenforrollforward; original winnerdigest cannot change. Strong ordinaryaccess/rawcommandsdeny beforelocalactivation evenif independentServinggateactive. StorageAccess::target_phase replacesearliernewrestore_preparation method (noalias); onlyActivate mayuseactivelease.
- Workspace diagnosticI alltargets/allfeatures PASS in2m26s: /tmp/kasumi-target-runner-workspace-check-i.log.
- MaterializationC PASS4 in40s: /tmp/kasumi-target-materialization-tests-c.log. Includes originalmat fence; nonTokiodropdrain; actual3engineproof->issuerfullfencedrain->localactivation->restart;3quorumcompletionrestart. Cases use realencryptedengine+issuer, explicitControl signingfixtures; fullnativecomposedissuerControltarget notyet.
- ACTIVE strictworkspaceClippyJ PTY returned by latestexec, log /tmp/kasumi-target-runner-clippy-j.log. Inspectprocess/log beforeeditingcompiledsources. Needconsumeprocess terminal. No sourceboundgate yet; diagnostics only.
- Remainingnative implementation mapped /tmp/kasumi-target-runner-draft/NATIVE-RUNNER-NEXT.md. Parent notified actual4pass andexplicitremainingnativejournal/runner/stop. Do notstoporFForiginal atenginecheckpoint.

## Inspection and original acquisition deadline (latest)
- Parent explicitly approved metadata-only InspectTarget under fresh independently committed current Control/Admin; separate proof, no mutation conversion. Implemented LifecyclePhase::InspectTarget, TargetInspectionInput {quorum, original_phase}, TargetReplicaInput::{Quorum,Inspection}, TargetInspectionObservation/SignedTargetInspection and private engine VerifiedTargetInspection. Exact original Complete/Activate phase and all3signedmaterializations bind new inspectioninput; actual current target quorum reconstructs original completion/activation fact, acceptance timestamp/identity unchanged. Missing completion is UnknownOutcome, never stop.
- open_target_replica now requires explicit TargetReplicaInput, no alias/fallback; inspection only opens already materialized+initialized original state. StorageAccess::target_phase allows active InspectTarget in addition to Activate. Issuer lifecycle_lease_view permits exact active target inspection. Native runtime servingpurpose selection for active inspection still needs implementation.
- RaftGroup::check_proposal rejects fresh application/retirement/custody/membership proposals under InspectTarget; ordinary engine/service phase checks likewise deny mutation. Existing committed/prior-accepted consensus replay remains separate from new proposal admission. TargetSigner::sign_inspection accepts only private VerifiedTargetInspection; separate domain and AuthenticatedTargetInspection, no sign_completed conversion. Platform read-only review found no new binding/drain/release defect, no composed-native claim.
- ClippyJ terminal two needlessQuestionMark targetservice expressions fixed. ClippyK terminal only result_large_err on fixture leader running_state debug collection, fixed to stringify debugstate. Logs preserved.
- InspectionA compile failed wrong fixture stop argument; preserved /tmp/kasumi-target-inspection-tests-a.log. B PASS1 /tmp/kasumi-target-inspection-tests-b.log: actual encrypted3replica Complete, never sign/publishack, originalComplete cap expires, freshInspect restarts all3 and recovers original fact; wronginput/phase/rawproposal/membership and stoppedepoch release negatives.
- Added opaque nonClone TargetRequestAdmission::capture(originalContext, timeout), check/run. Captures one suspend-aware elapsed deadline + async VerificationDeadline before remote acquisition; TargetOperationScope::begin_admitted consumes it once and requires exact RequestContext plus same_live_invocation Arc. Original TargetOperationScope::begin_operation captures explicitly at callsite for embedded; native must capture before anyremoteacquisition. Detached VerificationWork owns admission Arc, checks elapsedoriginaloperation + originalcredential + phase. Response fence checks exactoriginaloperation. No caller timestamp/reverifiedJWT can renew.
- Engine request admissionA/B compile fails Arc deref, preserved logs; C PASS1 /tmp/kasumi-target-request-admission-c.log: fake suspend-aware clock advances past25msoperation while500scredential stilllive; nextfuture notpolled, clockrollbackcannotreopen. Parent requested/approved this narrower delayed-acquisition test.
- MaterializationD PASS5 /tmp/kasumi-target-materialization-tests-d.log after deadline changes: actual first materialization now also tests capture1ms→10msremoteacqdelay→begin_admitteddenied beforemanifest; equal serialized verified credential with differentliveArc denied. Remaining3replica/activation/restart/drop/inspection tests pass. ExplicitControl signingfixtures, actual engine+issuer quorum.
- ACTIVE strictworkspaceClippyL PTY679? Consult latest tool response for actualsession; log /tmp/kasumi-target-runner-clippy-l.log. Do not edit compiled tree untilterminal. No finalsourceboundgate yet.

Native drafts (off-tree, NOT integrated/compiled): /tmp/kasumi-target-runner-draft/{target_runtime_config,target_phase_runtime,target_journal}.rs and TARGET-JOURNAL-NEXT.md.
- target_runtime_config requirednullableRuntimeConfig.target_recovery intended, installed exactControlroot+pinnedroute/clientTLS, separatejournalTransit/path, canonicalgenerationroot, targetsignerPKCS8, boundedmetadata/generations/operations, explicitissuerAdminbearer env mapping. NothingaddedtoRuntimeConfigyet.
- target_phase_runtime concrete KasumiLifecycleClient currentobserve underoriginalControlbearer, installed KasumiAuthorityClient executeLifecycle with separateissuerAdmincredential, nodeboundLifecycleBootattempt/lease, RuntimeLease prepared/active, opaque originalgate/scope and renewal. It uses TargetRequestAdmission throughout. Needs RuntimeLease::gate getter. FreshControlrecheck repeated; no raw DTO/callback. Inspect active-serving selection stillneedsresolvedshape; renewalworkerfixed to close on anyfailedrenewal. Native/SDK notyetwired.
- target_journal draft open/prepare with closed StoragePurpose::TargetJournal (notaddedyet), immutableintent/originalroot/node/partition and reservedboundedoutcomeheadroom, streamingmetadataverification, per-incarnation permanentstop planned. Stop publication/cleanup methods notyetimplemented; remove draft placeholder through realownedcoordinator, no successstub. Requires VerifiedTargetStop.signed getter ifpersistingsignature; use exactissuerreceipt/manifest/currentStopLocal gate and permanenttombstone beforelocaldrain. Journalmethod validation and storage visit_bounded signature notyetchecked.
- Parent/platform alloriginalsourceholds unchanged; original native5174 only. No sharedtarget.
- Reviewed platform publication_source/publication_directory/provider read-only: no concrete newdefect found. Earlierdispatchdeadlinefix correctly folds everyregion/operationcap intoSourcePlanProof.deadline; HostedSession.before retainsit. Directoryowner+commandversion/CAS, sourceclosure+samebookkeeping+UnknownOutcome flows consistent; thisisboundedreviewonly.

## Follow-up native request fence and independent journal (latest)
- Referenced `kasumi-engine/src/target_journal.rs` now implements independently keyed metadata-only journal, exact installed Control root/node identity, shared per-store mutation owner, streaming startup verification, immutable normal phase identities, bounded count/byte accounting, and generation-reserved permanent target stop. Actual encrypted journal test A passed (16 exact concurrent prepares, count exhaustion, actual issuer StopTarget/full drain/StopLocal, encrypted reopen, no application file).
- `TargetOperationScope::begin_admitted` remains strict original invocation matching for initial acquisition. New `begin_followup` retains the new actual request credential/timeout in addition to the unchanged original live phase. `TargetOperation::context` and ordered `.prepare` now use the new request's original credential, while the original phase cap and gate remain mandatory. Operation.run monitors both fences; old proofs retain their exact original operation identity.
- Materialization E: 6 PASS/1 fixture assertion failure: issuer fixture argument100 is receipt capacity, not max lease (lease1000ms). Corrected late test clock101->1001. F compile failed missing `?` on ServingGate.is_prepared result; preserved log, fixed. G is/was running; inspect log/process before edits.
- Required signed `LifecycleLeaseClaims.application_purpose:Option<LeasePurpose>` now derives actual issuer current incarnation role. StopLocal=None; Activate=Serving only after actual issuer activation; InspectTarget can use exact current prepared/serving role. Renewal rejects role changes; LifecycleGate requires exact same storage serving role. This avoids a native InspectTarget fallback/downgrade query.
- Native drafts `/tmp/kasumi-target-runner-draft/target_runtime_config.rs` now include explicit target-only tenant templates and exact source incarnation->backup alias/key templates, independently keyed journal and installed node identity. RuntimeConfig required nullable field integration pending. `target_phase_runtime.rs` now retains original/current RPC Control observations, actual installed TLS/JWT/issuer calls and uses signed application purpose. Not yet integrated/compiled.
- Parent may own an isolated schema change-set read_set branch. Agreed immutable transaction read_set in effect digest runs only fresh ordered effect; separate current lookup admission envelope guards historical receipt disclosure. Never rerun pre-activation Snapshot at release (schema/policy epochs change). Original5174 and runner unchanged by parent work.

## Native runner integrated (unverified production tranche; current source authoritative)
- Materialization G PASS7/7 after required signed `application_purpose` role addition and true1000ms lease fixture correction. All original encrypted materialization/completion/activation/InspectTarget/journal/followup tests passed.
- Native runtime check A PASS (phase/config only, unused warnings); B failed missing production ring dependency; C failed inherited ring workspace declaration; D PASS server alltargets/allfeatures after explicit ring0.17, native runtime+RPC startup integration. SDK check E PASS server alltargets/allfeatures (28.88sec). E session85726 is terminal but may still need consume; D session47670 consumed. No source-bound target-runner gate or commit yet.
- New referenced server files: target_runtime_config.rs, target_phase_runtime.rs, target_runtime.rs, rpc_target.rs. RuntimeConfig.target_recovery required explicit nullable; example setsNone. Target-only hosts can have0 ordinary tenants. Runtime opens independent journal and configured template runner; native admin listener registers KasumiTargetRecovery; shutdown drains runner. Source database is not selected/opened by target templates.
- `TargetRecoveryRuntime` owns bounded call permits64 and generation owners, original request admission before remote calls; real installed pinnedTLS Control+issuer acquisition and renewal; original Control finite phase plus fresh RPC credentials; journal prepare before providers; actual Materialize/Start/Initialize/Complete/Activate/Inspect/Stop paths. Replies retain private engine proof+same operation/phase/current bearer through final native encoding/audit/release. Detached RPC tasks retain actual owner locks/permits; timeout is UnknownOutcome.
- Stop path acquires actual verified issuer TargetStop/full drain + finite StopLocal, persists permanent journal stop FIRST, then closes prior phases/Raft/actual detached work/stores, requires unique NodeStore ownership, and deletes only a fixed hash(tenant)+incarnation file under canonical installed root with directory fsync. No app provider is constructed by stop; missing-file replay rechecks permanent stop. Local cleanup signature is metadata-only exact original StopLocal/node/stopped receipt and observation time, not first acceptance time. Needs real native stop/deletion/restart tests before any claim.
- New engine TargetActivationObservation/SignedTargetActivation, actual proof signer sign_activated + serving signature verifier. Ordered activation now also checks exact committed membership/endpoints like Complete.
- Shared proto KasumiTargetRecovery.Execute, serving TargetRuntimeRequest/Step/Outcome protocol, and SDK KasumiTargetClient with mandatory pinnedTLS/Control+issuer trust/node id. Private TargetAcknowledgement cannot Deserialize and exposes immutable observed DTO only. It does not authorize future effects. All result variants are matched to requested step/current committed intent and required actual node signatures/three materializations; Started/Initialized are explicitly ephemeral local acknowledgements, not completion/serving proofs.
- Platform agent requested read-only review of frozen three server files {target_runtime,target_phase_runtime,rpc_target}.rs; no response yet. Self-review known fixes queued after review: rpc_target wraps execute success in auth.audit_result before distinguishing acceptance, so an audit failure there must map UnknownOutcome (split result first); bounded expired-generation eviction must close/drain owners and remove only when no other generation Arc exists, else keep tracked (prevents permanently full live map and untracked late group). No edits to those three since review request.
- Upcoming required work still explicit: native composed Control+issuer+target tests (including real Transit-backed materialization or honest journal-only fixture first); secure ordinary serving promotion after committed target activation; restart route/recovery of activated generations; bounded prune/cleanup receipts; final source-bound tests/Clippy/fmt. Current phase-backed target activation still does not attach ordinary DataRegistry serving. Do not claim full native runner acceptance or DR deployment from current checks.
- Parent isolated schema branch now15f35b2 basedd9, frozen broad gate running /tmp/kasumi-schema-fences-gate-a. Focused12engine+3service+1authenticatedAPI passed; original5174 unchanged. Bounded read-only schema_activation/schema_service review found no new concrete defect. Later integrate verified schema commit only after coordinated runner boundary, no original moves.

## 2026-09-08 continuation: projection and local serving startup
Original remains clean5174. Active tree /tmp/kasumi-target-runner remains uncommitted based d9; parent schema15f35 verified364PASS2ignored independently, do not touch original.
- Fixed native `await_target_task` maps both timeout and spawned JoinError to UnknownOutcome, preserving returned inner definite errors. `/tmp/kasumi-target-runtime-outcome-a.log` PASS2. Post-success audit/encoding/release mapsUnknown; prune_expired bounded owner removal previously fixed.
- Strict runtime ClippyA failed only large protocol enum variants; boxed request Inspect and all signed outcome variants, no wire byte change. Strict Clippy has not yet been rerun on latest sources.
- New target_projection.rs child of actual TargetJournal. Private journal-bound VerifiedTargetServingProjection only minted from actual private local activation/inspection + installed signer; retains exact issuer signed lifecycle lease, Control root/partition/node/original intent, signed actual node observation. Checks full graph, exact fresh ServingGate manifest/incarnation/epoch/node/checkpoint/issuer activation digest and permanent local stop. Separate per-generation 2*256KiB reservation for stop and activation before effects; projection <=MAX-4096. Streaming open verifies signatures without nested store read locking; point exposure additionally verifies retained intent/generation references.
- Atomic `serving/{tenant}` candidate pointer published with projection, strictly increasing source epoch. Permanent old projections remain. Bounded point discovery independent of retained generation history, pointer <=4096 inside reserved headroom. Candidate is metadata hint only, never grant. Latest all-target server/client check PASS `/tmp/kasumi-target-local-confirmation-check-a.log`.
- TargetRuntime records actual Activate/Inspect activation projection. Ordinary registry promotion/background restart still NOT wired.
- New bootstrap_target_serving.rs `open_serving_target` requires opaque projection + fresh ServingGate, opens only existing bootstrap, verifies exact materialization hashes/deployment, replays actual local committed prefix, compares retained activation/membership before data-registerable TargetServingReplica owner. Drop captures original runtime shutdown executor. No source provider/old Control JWT used.
- Initial startup testB failed because initial Raft metrics membership not published immediately. Corrected by durable local metadata (no weakened check). New required TargetActivationFact.position=TargetCommitPosition(index,term,leader_node_id,command_sha256), assigned only actual apply; validates revision=checkpoint.revision+1+index. raft/local_applied.rs returns private ConfirmedLocalApplication only after persisted local committed/applied coverage+same-position identity+membership check. This is historical local coverage, not fresh quorum/Admin.
- `/tmp/kasumi-target-serving-test-c.log` PASS1 actual three-voter activation + selected voter ordinary encrypted reopen + fresh grant expiry. Prior A compile await mistake/B metrics failure preserved.
- `Database::confirm_target_activation(op, SignedTargetActivation)` added: verifies foreign installed node signature, exact local immutable activation/completion, current original phase and actual persisted application/membership. No proposals. Private proof retains same op and original expected signature for repeated release. Native ConfirmActivation step and SDK validation integrated and all-target check passed. **InspectTarget local follower confirmation still needs implementation.**
- New follower negative/actual confirmations are frozen/running in session27489 `/tmp/kasumi-target-local-confirmation-test-a.log`, private target `/tmp/kasumi-custody-substrate-target`, jobs1 Rust1.97.1. Check terminal before edits.
- Config now rejects source backup keys reused as target app/custody key, in addition to journal/source runtime separation.
- Parent approved local-applied confirmation with exact persisted target state + membership + applied position, original current phase/issuer and independent projection. No DTO-only promotion. Parent schema15f fullgate archived product implementation/evidence/kasumi-schema-admission-20260908-a manifestSHA0717d39accec77d60a40f4a32406a4321b3586bc3a356e8033679a5834757b23.

## Immediate next implementation design
1. Finish follower confirmation test; strictClippy as useful, preserving all failed logs.
2. Metadata-only ConfirmInspection for followers after original Activate expiry, using a foreign signed exact InspectTarget observation + this voter's actual activation/applied cursor/current phase. Keep type separate and no mutation conversion.
3. Wire ordinary runtime background reconciliation from `journal.serving_candidate(tenant)` point lookups over bounded installed templates. Do not block ordinary data startup on expired management JWT; permanent projection + fresh current exact Serving lease is approved startup authority. First runtime transition may occur after original reply/operation drops; scope semaphore must report no active operation so close/drain cannot invalidate a still-releasing reply. No in-place phase→serving storage bypass. Close old group/scope, uniquely drop stores, then acquire fresh Serving lease before app provider, use open_serving_target, register only actual verified database. Actual Activate acknowledgement remains immutable commit fact, not a serving-ready claim.
4. Generation closure must remove exact registered DataRegistry Arc AND replace its InstalledRetirementSource::Serving route with typed RecoveringControl before dropping app owners; registry retained source Arc otherwise prevents unique file drain. Do not discard terminal custody authority: dynamic serving targets which later retire need custody-only open/recovery using installed target custody provider + actual ControlLog, no app provider. Current baseline runtime open_retired_source helper handles exact original replicated group; target templates must integrate this safely. Only currently pointed generation is easy first iteration; older terminal history may need separate bounded journal discovery, do not silently claim all recovery routes complete.
5. Need actual native composed pinnedTLS Control+issuer+3target tests, metadata-only stop with unavailable application/source key tests, ordinary old dataJWT denial after serving promotion, full sourcebound workspace strictgate, clean scoped commit. Original remains5174 until parent handoff. No full runner acceptance yet.

## Latest runtime continuation (2026-09-08)
- Actual follower confirmation test PASS `/tmp/kasumi-target-local-confirmation-test-a.log` (both actual followers confirm own committed state, forged position/signature denied, selected voter serving reopen+expiry included). Session27489 consumed.
- `TargetRuntimeStep::ConfirmInspection` + engine private `confirm_target_inspection` + SDK now implemented; current all-target check PASS `/tmp/kasumi-target-inspection-confirmation-check-a.log`. Still needs actual active-target inspection/follower regression.
- **Ordinary runtime background reconciliation is now wired**, not merely draft: `target_serving_runtime.rs` child module. It waits for scope.is_idle (original reply retains operation slot), drains original phase/group, independently validates journal projection, reads only custody provider/ControlLog first, and opens closed current `RetiredCustody` on actual retired metadata. Otherwise obtains fresh Serving lease before app provider, calls `open_serving_target`, registers peer route + exact ordinary DataRegistry. Generation tracks serving owner/lease/data Arc/custody owner/probe; close detaches exact registry Arc into RecoveringControl, unregisters group, joins actual Database/Custody/phase, requires unique NodeStore.
- Native runtime constructor now takes `DatabaseRegistry`; NodeRuntime caller updated. Runtime shutdown joins its serving monitor and actual generations. Failed/expired empty owners are conservatively pruned only after full drain/no other Arc owners. Healthy custody generations are retained independently of expired serving gates.
- Parent read-only reviewed native background/Generation close/registry exact detach/engine owner and found no authority bypass. Required actual tests for errors after peer registration/before data insert and shutdown/retry remain outstanding.
- Parent found silent background errors + linear-from-start page iteration. Both fixed: BTreeMap::range pages (16 installed templates), bounded128 failure stage cache, deduplicated globally rate-limited warnings with only tenant+stage (no error chains/tokens/paths). Recovery info once on transition. Each warning stage identifies Projection/Ownership/Custody/Issuer/ApplicationKeys/LocalReplay/PeerRegistration/DataRegistration.
- **Strict server/client all-target/all-feature Clippy PASS** `/tmp/kasumi-target-serving-runtime-clippy-b.log`, session53933 consumed. Prior runtimeClippyA boxed protocol, B boxed ResponseEvidence; servingRuntimeClippyA alsoPASS before ConfirmInspection/health. There is no active Cargo after53933 consumed unless a newer call begins.
- Parent schema15f checkpoint verified/archived; original5174 untouched. Must integrate verified schema later before final combined native gate after clean boundary.

## Native composed fixture plan (next immediate work)
Need actual native tests now. Use new `rpc_target_tests.rs` with a reusable protocol fixture copied/refactored from rpc_lifecycle_tests.rs: actual three-voter encrypted Control and issuer backends (existing InProcess internal quorum fixture), combined pinnedTLS NativeLifecycleControl+NativeAuthority endpoint, actual JWT issuer with explicit resource and node identities, installed current Control lifecycle schema. New target replicas themselves should use real `ClusterNetwork` with three pinned mutual-TLS peer listeners (actual composed target transport proof). Generate CA/leaf PEM+private-key files for localhost+node-N and save private permissions0600 so RuntimeTargetPhase/RuntimeLease use actual config connector. No fake opaque proof construction.
- Runtime target fixture uses actual HTTPS Transit fixture server (baseline runtime::lifecycle_tests has mock implementation around2090; extract/copy test-only helper). Actual encrypted source backup created through engine using same configured Source Transit key, then source Database/keys shut down before target operations. Mark local KMS fixture external boundary, not deployed KMS.
- Construct explicit RuntimeConfig/TenantRecoveryConfig with one source lineage+fixed destination alias, independent journal key/path, separate target app/custody keys, three fixed peers, current source-independent Control and issuer endpoints/TLS/node JWT env. Avoid defaults/shims.
- First composed test: permanent StopTarget before materialization, actual Control StopLocal intent and issuer drain; target runtime closure's source/app credentials deliberately unavailable while journal/node/admin credentials available. Native target SDK cleanup must succeed and replay/reopen, proving no application/source provider construction. Bad resource/current revocation/expiry deny. Actual checkpoint can be real source backup, no shape-only hash fixture claim.
- Next composed happy path: native Control/issuer PrepareTarget, Materialize three actual replicas, complete signatures, Initialize, Complete, source Fence+fullmax drain, issuer ActivateCommitted, native target Activate+ConfirmActivation followers, background serving, old Data JWT denied, new incarnation data works. Restart target runtime with old Control JWT/source app key unavailable; independent journal+current Serving startup succeeds. Missing/corrupt projection/local applied state denies exposure. Then fresh InspectTarget+ConfirmInspection repair missing projection after original Activate expires. Current target custody retirement/restart native proof recovery, registration-failure/shutdown/retry owner drain negatives.
- Source/local engine fixture methods must remain explicitly distinguished from native/transport proof in final sourcebound receipt. Entire workspace test/strictClippy/fmt and exact source hashes/failed logs retained; no final checkpoint/FF yet.
