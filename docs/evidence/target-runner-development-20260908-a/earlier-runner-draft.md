# Native target runner integration after current engine diagnostics

This is the remaining required implementation, not a released capability. Original native remains5174; only active target-runner worktree changes.

1. Add explicit required nullable RuntimeConfig.target_recovery. Its Some config binds installed remote Control root+TLS endpoint, local target attestation key, independent target-journal Transit policy/path, bounded generation root/record limits and tenant recovery source aliases/keys. Request JSON never chooses Control/issuer URL/key/token/source filesystem. All configured target placements must match actual replication peers/failure domains and local node identity.
2. Dedicated native/SDK service. Verify exact actual mTLS+original Control-resource JWT; retain original bearer/private RequestContext through response. Concrete installed KasumiLifecycleClient observes exact current-quorum intent under that same token, followed by installed issuer acceptance/acquireLifecycle pre-request elapsed anchoring. No callback/DTO asserts current admin/commit. Local target phase RuntimeLease renewal preserves original gate/JWT cap and checks prior true deadline. Data/Admin endpoint never reinterprets old source token.
3. Independently encrypted bounded per-node TargetJournal purpose. Before application-provider creation, persist exact original intent+input+node/install/origin. Local serialization and immutable records prevent late/changed reuse. Missing record or lease expiry is not stop proof. Materialization signatures published only after actual engine verification; unknown publication is recovered by fresh exact graph readback. Journal records contain metadata/attestations, never application payload. Current metadata recovery rechecks installed independent Control quorum/Admin and original request credential at final release; it does not require source Data quorum or revive an old phase.
4. Per-generation runtime owner retains scope, original-operation fences, serving+phase leases, storage domains and TargetReplica. Initialize registers3authenticated peers then designated membership; Complete and localActivate invoke closed engine prefixes. Reopen only after actual old owner/group/store drain; original handles stay closed. Ordinary route can open only with independently active serving lease AND committed local activation. Lease renewal does not replace original JWT/operation deadline.
5. StopLocal must acquire actual independent incarnation-wide TargetStop proof/full immutable drain (StopActivation insufficient), then close and drain every local scope/worker/group before cleanup. Persist permanent local tombstone before granting a late queued materialization any provider access. Hold native operation/work reservation through actual final file close/deletion. Lost ack/restart replays exact tombstone or prior Activated winner; no timeout/inferred provider result as proof. Generation path derived from installed root+canonical incarnation only; no generic tenant payload administration bypass.
6. Tests: actual pinned TLS target service, real three-replica issuer+Control+target group, original/stale Data credentials, exact graph/hashes across restore, startup/provider cancellation, current Control revocation and queued expiry, incarnation-wide stop preceding/mid-materialization and old queued phases, deletion only after non-runtime owner shutdown/work drain, permanent outcome/lost reply/encrypted restart. Source-independent recovery never downgrades/local quorum; healthy path still needs exact source retirement proof. External independently deployed authority/KMS/PKI/clock assumptions remain explicit.

Engine APIs now exist/compile: TargetMaterializationConfig, TargetOperationScope, TargetLifecycleInvocation, TargetSigner; materialize_target_replica; open_target_replica->TargetReplica (owned shutdown runtime+work); TargetReplica::initialize; Database::complete_target/observe_target_completion -> private VerifiedTargetCompletion; Database::activate_target/observe_target_activation -> private VerifiedTargetActivation. New StorageAccess::target_phase (no alias) allows prepared or Activate-phase active gate. TargetQuorumInput binds exhaustive3actualsignedmaterializations. CommittedActivation requires actual SignedTargetCompletion and ActivateTargetInput completionSHA. No native runner/SDK implementation yet.
