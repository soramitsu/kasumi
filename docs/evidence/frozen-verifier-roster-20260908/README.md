# Frozen physical verifier roster

At `c1dc8b0`, authority tests pass 42, the actual native pinned TLS case passes,
and strict workspace Clippy, fixture-free server checks and formatting pass.
The roster retains physical endpoint identities, explicit Control root/partition/
node enrollment facts, bounded encrypted roster hashing and current-admin
pagination. Staging freezes new old-generation admissions and queued releases.

The Control enrollment is still an administrative fact. Current live Control
binding, remote publication acknowledgments/revocations and full issuer drain
remain incomplete, so global retirement cannot complete. Initial compile and
intermediate logs are retained; these focused macOS gates do not certify release.
