# Failed-opening recovery source provenance, revision 8

This source-only successor binds the frozen 47-path recovery candidate and its
26 changed redb paths. The resulting vendor inventory contains 110 regular
files, including the new retained-opening admission integration test. No native
qualification or release acceptance is claimed here. The sole revision-7-to-8
source correction adds audit_archive_owned.rs as a fixture-only 47th proposal
path. Its initial and repeated node-shutdown assertions now require Retained;
all original retained charge, issue identity, canceled/accepted census and
inode/content assertions remain. The prior 46 proposals, all production and
redb source bytes, and vendor inventory rows remain unchanged. This successor
binds the exact revision 8 candidate. Native07 and its exact-binary rerun failed
at the stale Complete expectation; those trials remain preserved and are not
promoted to passing results.

The published crate checksum, upstream commit, companion archive checksum and
file-level attribution are preserved. The previous PageNumber provenance stays
immutable at its original path. Each inherited row identifies that exact prior
record; earlier checkpoint review metadata is historical, not a claim about the
new bytes. The two removed upstream test files remain recorded as removed.

KASUMI_PATCH.md is included in the exact redb subtree inventory and carries the
candidate's frozen bytes. Top-level vendor support files and all other dependency
inventories remain unchanged. Source, provenance and policy must advance together.

The copied candidate manifest and patch retain their exact original bytes. Its
README records remaining ownership and resource gaps. Native checks, final Cargo
selection, and the required upstream container, audit and fuzz gates remain open.
