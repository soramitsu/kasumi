# Failed opening recovery revision8

This immutable successor adds one fixture-only path to the unchanged 46-path revision7 proposal. The archive publication regression now requires Retained on both initial and repeated failed node shutdown. It preserves all original issue Arc identities, rejected new reads, exact installed file-attempt/count/charge/parent assertions, canceled-census retention, accepted-census release, inode/content and same-disk reopen assertions. No production behavior changes and no test is renamed or removed.

Revision7/native07 completed the unfiltered 366-name store suite: 363 passed, one failed at the stale Complete expectation, and two existing tests were ignored. A single guarded run of that exact original binary reproduced the same failure without Cargo (0 passed, one failed, 365 filtered). All frozen native07 inputs, tools, controls, binaries and emitted artifacts remained unchanged; every process group drained. Later assertions were not executed in those failed trials. All evidence is preserved.

The consuming NodeDatabase close API still lacks an independent physical disposition on Storage failure, so its conservative sticky Retained result remains required. Migration to retained owner/report recovery is an explicit G02 obligation, not satisfied by this fixture correction. Implicit spool/snapshot constructors/destructors, worker/cache/opaque outcomes/allocator/RSS, full workspace/public vendor/fuzz/container qualification and release acceptance remain open. New native and exact provenance qualification are pending; expected inventories remain 366 store, 231 vendor unit and one production admission integration test.
