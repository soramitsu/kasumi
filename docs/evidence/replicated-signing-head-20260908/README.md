# Replicated authority signing head

Intermediate macOS ARM64 evidence for `5859d8e`, integrated at `7ada53f`.
Explicit stage/activation operations retain permanent outcomes and fence old
issuer generations through replicated state. Current authenticated administration
can resolve activation while an old operational signer is sealed.

All 40 authority tests, an actual pinned TLS transition, strict workspace Clippy,
fixture-free production checks and formatting passed. `evidence.json` binds the
source, logs and executables; the initial malformed-fixture helper failure is
preserved. Every listed log was hash-checked when copied here.

Complete verifier enrollment, remote acknowledgments/revocations, issuer drains
and safe stage abort remain separate unfinished work. This checkpoint cannot
complete global retirement and is not final release acceptance.
