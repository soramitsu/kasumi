# Control audit and exact phase outcome fixtures

The retained manifests/logs distinguish each source and every failed attempt.
Audit exhaustion now requires an exact ordered capacity rejection and absolute
sequence progress, rather than treating any error or unchanged hot-vector size
as exhaustion. Ambiguous recovery phase acknowledgements resolve their original
permanent identities. All seven lifecycle tests passed; strict Clippy passed
after a final needless-borrow fixture correction.

Test executable hashes were not captured before the shared target was rebuilt;
that evidence is explicitly unavailable. The seven-test pass predates the
borrow-only correction. These focused checks are not final-source Linux gates.
