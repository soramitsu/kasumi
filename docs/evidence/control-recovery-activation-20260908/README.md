# Control recovery activation checkpoint

Source 8c99af9 persists exact issuer activation inputs and the immutable original
attempt before dispatch. An ambiguous or expired attempt is resolved through an
ordered signed StopActivation outcome. A committed winner always proceeds
forward; permanent negative evidence permits stopping the target. Fresh finite
activation authorization starts all three voters and verifies their signed local
confirmations. Planned source retirement receives its deadline when that phase
is admitted, after target completion. Exact physical verifier identity is part
of issuer voter comparison.

Ten replicated lifecycle tests and fifteen snapshot tests pass, together with
focused authority/types regressions, strict workspace Clippy, all-target checks,
fixture-free server checks and formatting. Raw initial compiler failures and
source/executable hashes remain in the adjacent evidence. The replicated tests
use encrypted files and in-memory transport; the new actual target-process
StartActivation path is compiled but has not run in a native process gate.

Atomic route publication and expired original Complete proof resolution remain
unfinished. These checks do not close final source-unavailable recovery, physical
cleanup, cross-platform or release endurance acceptance.
